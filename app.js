const $=(s,c=document)=>c.querySelector(s);
const $$=(s,c=document)=>[...c.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));

const icons={
 menu:'<path d="M4 6h16M4 12h16M4 18h16"/>',cart:'<circle cx="9" cy="20" r="1"/><circle cx="19" cy="20" r="1"/><path d="M3 4h2l2.5 11h11l2-8H6"/>',users:'<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.9M16 3.1a4 4 0 0 1 0 7.8"/>',tag:'<path d="M20.6 13.6 11 23.2l-9-9V4h10.2z"/><circle cx="7" cy="9" r="2"/>',box:'<path d="m21 8-9-5-9 5 9 5 9-5Z"/><path d="m3 8 9 5 9-5v8l-9 5-9-5Z"/><path d="M12 13v8"/>',store:'<path d="M3 9 5 3h14l2 6M5 13v8h14v-8"/><path d="M3 9a3 3 0 0 0 6 0 3 3 0 0 0 6 0 3 3 0 0 0 6 0"/>',wallet:'<rect x="2" y="5" width="20" height="15" rx="2"/><path d="M16 13h6M2 9h16"/>',chart:'<path d="M3 3v18h18"/><path d="m7 16 4-5 4 3 5-7"/>',badge:'<rect x="3" y="4" width="18" height="16" rx="2"/><circle cx="9" cy="10" r="2"/><path d="M15 8h3M15 12h3M7 16h10"/>',settings:'<circle cx="12" cy="12" r="3"/><path d="M19.4 15a2 2 0 0 0 .4 2.2l-2.6 2.6a2 2 0 0 0-2.2-.4 2 2 0 0 0-1.3 1.6h-3.4A2 2 0 0 0 9 19.4a2 2 0 0 0-2.2.4l-2.6-2.6a2 2 0 0 0 .4-2.2A2 2 0 0 0 3 13.7v-3.4A2 2 0 0 0 4.6 9a2 2 0 0 0-.4-2.2l2.6-2.6A2 2 0 0 0 9 4.6 2 2 0 0 0 10.3 3h3.4A2 2 0 0 0 15 4.6a2 2 0 0 0 2.2-.4l2.6 2.6A2 2 0 0 0 19.4 9a2 2 0 0 0 1.6 1.3v3.4A2 2 0 0 0 19.4 15Z"/>',help:'<circle cx="12" cy="12" r="10"/><path d="M9.1 9a3 3 0 1 1 5.8 1c0 2-3 2-3 4M12 18h.01"/>',plus:'<path d="M12 5v14M5 12h14"/>',search:'<circle cx="11" cy="11" r="8"/><path d="m21 21-4.4-4.4"/>',filter:'<path d="M4 5h16M7 12h10M10 19h4"/>',down:'<path d="m6 9 6 6 6-6"/>',up:'<path d="m18 15-6-6-6 6"/>',close:'<path d="m18 6-12 12M6 6l12 12"/>',edit:'<path d="M12 20h9M16.5 3.5a2.1 2.1 0 0 1 3 3L8 18l-4 1 1-4Z"/>',eye:'<path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7S2 12 2 12Z"/><circle cx="12" cy="12" r="3"/>',download:'<path d="M12 3v12m0 0 5-5m-5 5-5-5M5 21h14"/>',upload:'<path d="M12 21V9m0 0 5 5m-5-5-5 5M5 3h14"/>',cash:'<rect x="2" y="5" width="20" height="14" rx="2"/><circle cx="12" cy="12" r="3"/><path d="M6 9h.01M18 15h.01"/>',qr:'<rect x="3" y="3" width="7" height="7"/><rect x="14" y="3" width="7" height="7"/><rect x="3" y="14" width="7" height="7"/><path d="M14 14h3v3h-3zM18 18h3v3h-3zM18 14h3M14 18v3"/>',transfer:'<path d="m17 3 4 4-4 4M3 7h18M7 21l-4-4 4-4M21 17H3"/>',check:'<path d="m20 6-11 11-5-5"/>',trash:'<path d="M3 6h18M8 6V4h8v2M19 6l-1 15H6L5 6M10 11v6M14 11v6"/>',monitor:'<rect x="2" y="3" width="20" height="14" rx="2"/><path d="M8 21h8M12 17v4"/>',calendar:'<rect x="3" y="5" width="18" height="16" rx="2"/><path d="M16 3v4M8 3v4M3 11h18"/>',receipt:'<path d="M4 2v20l4-2 4 2 4-2 4 2V2l-4 2-4-2-4 2Z"/><path d="M8 9h8M8 13h6"/>'
};
const svg=n=>`<svg viewBox="0 0 24 24" aria-hidden="true">${icons[n]||icons.box}</svg>`;
icons.globe='<circle cx="12" cy="12" r="9"/><path d="M3 12h18M12 3a15 15 0 0 1 0 18M12 3a15 15 0 0 0 0 18"/>';
const injectIcons=(c=document)=>$$('[data-icon]',c).forEach(e=>e.innerHTML=svg(e.dataset.icon));

const seed={
 settings:{businessName:'GRASSI',legalName:'Grassi Distribuidora',nit:'1029384756',phone:'+591 700 00000',city:'Cobija, Pando',currency:'Bs',language:'es',accent:'#3b82f6',wholesale:true,options:{soundOrders:true,printReceipt:true,autoCode:true,quickProduct:true,duplicateCode:false,blockNoStock:true,autoPrice:false},appearance:{mode:'light',palette:'blue',accent:'#3b82f6',shell:'nex'},posLayout:{dock:'sidebar',density:'comfortable',theme:'touch',items:['client','wholesale','delivery','notes','payment']},cashClosing:{automatic:false,time:'00:00'}},
 products:[],
 clients:[],
 employees:[
  {id:'F001',name:'Funcionario Demo Caixa',role:'Cajero',phone:'',document:'',email:'employee@example.invalid',sales:0,total:0,ticket:0,goal:0,admin:false,supervisor:true,photo:''},
  {id:'F002',name:'Funcionario Demo Mayorista',role:'Vendedor mayorista',phone:'',document:'',email:'seller@example.invalid',sales:0,total:0,ticket:0,goal:0,admin:false,supervisor:false,photo:''}
 ],
 sales:[],cash:[],closings:[],offlineQueue:[],dataVersion:3
};

const key='grassi.pdv.nex.v2';
let state=(()=>{try{return {...structuredClone(seed),...JSON.parse(localStorage.getItem(key)||'{}')}}catch{return structuredClone(seed)}})();
state.settings={...structuredClone(seed.settings),...(state.settings||{}),options:{...seed.settings.options,...(state.settings?.options||{})},appearance:{...seed.settings.appearance,...(state.settings?.appearance||{})},posLayout:{...seed.settings.posLayout,...(state.settings?.posLayout||{})},cashClosing:{...seed.settings.cashClosing,...(state.settings?.cashClosing||{})}};
if(!state.inventoryResetV3){try{localStorage.setItem('grassi.pre-reset.v3',JSON.stringify({products:state.products||[],savedAt:new Date().toISOString()}))}catch{}state.products=[];state.stockMovements=[];state.dataVersion=3;state.inventoryResetV3=true;try{localStorage.setItem(key,JSON.stringify(state))}catch{}}
if(!state.operationalResetV13){
 try{localStorage.setItem('grassi.pre-reset.v13',JSON.stringify({clients:state.clients||[],products:state.products||[],sales:state.sales||[],cash:state.cash||[],closings:state.closings||[],stockMovements:state.stockMovements||[],savedAt:new Date().toISOString()}))}catch{}
 state.clients=[];state.products=[];state.sales=[];state.cash=[];state.closings=[];state.stockMovements=[];state.offlineQueue=[];
 state.employees=(state.employees||[]).map(employee=>({...employee,sales:0,total:0,ticket:0,goal:0}));state.operationalResetV13=true;
 try{localStorage.setItem(key,JSON.stringify(state))}catch{}
}
state.offlineQueue=Array.isArray(state.offlineQueue)?state.offlineQueue:[];
const appMode=document.body.dataset.mode||'admin';
const storedView=sessionStorage.getItem('grassi.lastView');
let authSession=null;
let ui={view:storedView||'sales',menu:false,search:'',salesTab:'history',salesPayment:'Todos',salesPeriod:'7 días',saleMode:'Venta',clientFilter:'Todos',clientSort:'Nombre',productTab:'items',productCategory:'Todas',productFilter:'Todos',productSort:'Nombre',stockTab:'stock',stockFilter:'Todos',cashFilter:'Todos',employeeFilter:'Todos',report:'Resumen',reportPeriod:'7 días',reportGroup:'Día',settingsTab:'brand',clientProfileTab:'account',activeClientId:'',tutorialStep:0,cart:[],posSearch:'',posCategory:'Todos',posMobileView:'products',posSidebarCollapsed:false,posQty:1,posClient:'',wholesale:false,payment:'Efectivo',posNotes:'',posDelivery:'Retiro en tienda',importType:'backup',draggedWidget:'',lastReceipt:null};
const save=()=>{localStorage.setItem(key,JSON.stringify(state));applyBrand()};
const locale=()=>state.settings.language==='pt'?'pt-BR':'es-BO';
const money=v=>`${state.settings.currency||'Bs'} ${Number(v||0).toLocaleString(locale(),{minimumFractionDigits:2,maximumFractionDigits:2})}`;
const date=v=>new Intl.DateTimeFormat(locale(),{day:'2-digit',month:'2-digit',year:'numeric'}).format(new Date(v));
const time=v=>new Intl.DateTimeFormat(locale(),{hour:'2-digit',minute:'2-digit'}).format(new Date(v));
const initials=n=>n.split(/\s+/).slice(0,2).map(x=>x[0]).join('').toUpperCase();
const sum=(arr,k='amount')=>arr.reduce((n,x)=>n+Number(x[k]||0),0);
const avatar=(p,n,cls='')=>`<span class="avatar ${cls}" ${p?`style="background-image:url('${esc(p)}');color:transparent"`:''}>${initials(n)}</span>`;
const palettes={blue:'#3b82f6',green:'#16a37a',purple:'#765bd8',orange:'#e9822d',red:'#d94b5b',grassi:'#e20b13'};
const paletteNames={blue:'Azul',green:'Verde',purple:'Violeta',orange:'Naranja',red:'Rojo',grassi:'GRASSI Repuestos'};
const ptText={
 'Ventas':'Vendas','Clientes':'Clientes','Productos':'Produtos','Inventario':'Estoque','Catálogo Online':'Catálogo online','Caja':'Caixa','Informes':'Relatórios','Funcionarios':'Funcionários','Configuración':'Configurações','Ayuda y soporte':'Ajuda e suporte',
 'Abrir terminal PDV':'Abrir terminal PDV','Nueva Venta - F3':'Nova venda - F3','Nuevo Pedido':'Novo pedido','Nuevo Presupuesto':'Novo orçamento','Historial':'Histórico','Pedidos abiertos':'Pedidos abertos','Presupuestos':'Orçamentos','Acción':'Ação','Número':'Número','Resumen':'Resumo','Fecha':'Data','Hora':'Hora','Ítems':'Itens','Cliente':'Cliente','Vendedor':'Vendedor','Subtotal':'Subtotal','Valor pagado':'Valor pago','Pago':'Pagamento','Total':'Total',
 'Nuevo cliente':'Novo cliente','Nombre':'Nome','Saldo':'Saldo','Compras':'Compras','Ver cuenta':'Ver conta','Débito / Crédito':'Débito / Crédito','Estado':'Status','Código':'Código','Observación':'Observação','Ciudad':'Cidade','Celular':'Celular','Tipo':'Tipo','Con deuda':'Com dívida','Con crédito':'Com crédito','Activo':'Ativo',
 'Artículos':'Produtos','Etiquetas':'Etiquetas','Proveedores':'Fornecedores','Nuevo producto':'Novo produto','Precio de venta':'Preço de venda','Precio mayorista':'Preço de atacado','Stock actual':'Estoque atual','Costo':'Custo','Categoría':'Categoria','Unidad':'Unidade','Gestionar categorías':'Gerenciar categorias','Todas':'Todas','Todos':'Todos','Stock bajo':'Estoque baixo','Sin stock':'Sem estoque',
 'Stock por producto':'Estoque por produto','Transacciones':'Movimentações','Movimiento':'Movimentação','Reservado':'Reservado','Disponible':'Disponível','Valor del inventario':'Valor do estoque','Nueva transacción':'Nova movimentação','Cerrar caja':'Fechar caixa','Saldo inicial':'Saldo inicial','Entradas':'Entradas','Salidas':'Saídas','Saldo esperado':'Saldo esperado','Entrada':'Entrada','Salida':'Saída',
 'Empresa y marca':'Empresa e marca','Apariencia':'Aparência','Editor de PDV':'Editor do PDV','Importar / Exportar':'Importar / Exportar','Guardar cambios':'Salvar alterações','Aplicar apariencia':'Aplicar aparência','Modo de pantalla':'Modo de tela','Claro':'Claro','Oscuro':'Escuro','Automático':'Automático','Paleta principal':'Paleta principal','Vista previa':'Visualizar','Guardar layout':'Salvar layout',
 'Venta':'Venda','Buscar producto':'Buscar produto','Elegir cliente':'Escolher cliente','Cantidad':'Quantidade','Cobrar':'Finalizar','Añadir resultado':'Adicionar resultado','Quitar último':'Remover último','Atajos':'Atalhos','Observaciones':'Observações','Entrega':'Entrega','Ningún artículo':'Nenhum item','Ningún artículo añadido':'Nenhum item adicionado','Finalizar compra':'Finalizar compra','Volver':'Voltar','Continuar al pago':'Continuar para o pagamento','Confirmar pago':'Confirmar pagamento','Total a cobrar':'Total a cobrar','Valor recibido':'Valor recebido','Efectivo':'Dinheiro','Transferencia':'Transferência','Cuenta cliente':'Conta do cliente','Nueva venta':'Nova venda','Imprimir cupón':'Imprimir cupom','Venta registrada':'Venda registrada','Pendiente de sincronización':'Pendente de sincronização','En línea':'Online','Sin conexión':'Sem conexão','Modo local':'Modo local',
 'Catálogo de productos':'Catálogo de produtos','Productos visibles para venta rápida':'Produtos visíveis para venda rápida','Todos los productos':'Todos os produtos','Toque para añadir':'Toque para adicionar','Carrito de venta':'Carrinho da venda','Código, nombre o use el lector de código de barras':'Código, nome ou use o leitor de código de barras','El inventario está vacío. Registre sus productos desde el panel administrativo.':'O estoque está vazio. Cadastre seus produtos pelo painel administrativo.','Busque por nombre, código o EAN para comenzar':'Busque por nome, código ou EAN para começar','Descuento':'Desconto','Retiro en tienda':'Retirada na loja','Sesión':'Sessão','Atajos rápidos':'Atalhos rápidos','Forma de pago':'Forma de pagamento','Cupón':'Cupom','Consumidor final':'Consumidor final','Sin observaciones':'Sem observações','Total de la compra':'Total da compra','Recibido':'Recebido','Cambio':'Troco','Guardado en este dispositivo':'Salvo neste dispositivo','Sincronizado':'Sincronizado','Cant.:':'Qtd.:','Toque para editar':'Toque para editar',
 'Correo electrónico':'E-mail','Contraseña':'Senha','Entrar y abrir caja':'Entrar e abrir o caixa','Entrar al sistema':'Entrar no sistema','Accesos de demostración':'Acessos de demonstração','Administrador':'Administrador','Funcionario':'Funcionário','Cerrar sesión':'Sair','Mi sesión':'Minha sessão','Cambiar modo visual':'Alterar modo visual','Idioma':'Idioma',
 'Cuenta':'Conta','Datos':'Dados','Cuenta corriente':'Conta corrente','Concepto':'Descrição','Valor':'Valor','Situación final':'Situação final','Ventas e historial':'Vendas e histórico','Ficha del cliente':'Ficha do cliente','Crédito disponible':'Crédito disponível','Cuenta al día':'Conta em dia','Debe':'Deve','Total comprado':'Total comprado','Editar datos':'Editar dados','Línea de tiempo':'Linha do tempo','Sin actividad todavía':'Ainda sem atividade','Las ventas y movimientos aparecerán aquí.':'As vendas e movimentações aparecerão aqui.','Cuenta sin movimientos':'Conta sem movimentações','Agregar deuda':'Adicionar dívida','Saldo a favor':'Saldo a favor','Cobro parcial o total':'Cobrança parcial ou total','Corregir':'Corrigir','Ajustar saldo':'Ajustar saldo',
 'Centro de ayuda':'Central de ajuda','Cuéntenos qué necesita antes de continuar a WhatsApp.':'Conte o que precisa antes de continuar para o WhatsApp.','Categoría':'Categoria','Problema técnico':'Problema técnico','Duda de uso':'Dúvida de uso','Solicitud / requisito':'Solicitação / requisito','Asunto':'Assunto','Descripción detallada':'Descrição detalhada','Continuar en WhatsApp':'Continuar no WhatsApp','El mensaje se abrirá listo para revisar. Usted decide cuándo enviarlo.':'A mensagem será aberta pronta para revisão. Você decide quando enviá-la.'
};
function translateText(value){if(state.settings.language!=='pt')return value;const lead=value.match(/^\s*/)?.[0]||'',trail=value.match(/\s*$/)?.[0]||'',core=value.trim();if(!core)return value;if(ptText[core])return lead+ptText[core]+trail;return lead+core.replace(/^Mostrando /,'Mostrando ').replace(/ transacciones$/,' movimentações').replace(/^Cantidad: /,'Quantidade: ').replace(/ artículo(s)?$/,' item$1').replace(/^Hecho por /,'Atendido por ').replace(/^Período: /,'Período: ').replace(/^Pago: /,'Pagamento: ')+trail}
function applyLanguageDOM(root=document){document.documentElement.lang=state.settings.language==='pt'?'pt-BR':'es';if(state.settings.language!=='pt')return;const walker=document.createTreeWalker(root,NodeFilter.SHOW_TEXT);const nodes=[];while(walker.nextNode())nodes.push(walker.currentNode);nodes.forEach(node=>{if(!['SCRIPT','STYLE'].includes(node.parentElement?.tagName))node.nodeValue=translateText(node.nodeValue)});$$('input[placeholder],textarea[placeholder]',root).forEach(el=>{el.placeholder=translateText(el.placeholder)});$$('[title]',root).forEach(el=>{el.title=translateText(el.title)});$$('[aria-label]',root).forEach(el=>{el.setAttribute('aria-label',translateText(el.getAttribute('aria-label')))})}
function languageSelector(compact=false){return `<label class="language-switch ${compact?'compact':''}" title="Idioma"><span>${svg('globe')}</span><select data-language aria-label="Idioma"><option value="es" ${state.settings.language!=='pt'?'selected':''}>ES</option><option value="pt" ${state.settings.language==='pt'?'selected':''}>PT</option></select></label>`}
function applyBrand(){
 const appearance=state.settings.appearance||{},mode=appearance.mode||'light',resolved=mode==='auto'?(matchMedia('(prefers-color-scheme: dark)').matches?'dark':'light'):mode,accent=palettes[appearance.palette]||appearance.accent||state.settings.accent||'#3b82f6';
 document.documentElement.dataset.theme=resolved;document.documentElement.dataset.palette=appearance.palette||'blue';document.documentElement.dataset.shell=appearance.shell||'nex';document.documentElement.style.setProperty('--accent',accent);document.documentElement.style.setProperty('--accent-rgb',hexRgb(accent));
 document.querySelector('meta[name="theme-color"]')?.setAttribute('content',accent);document.title=`${state.settings.businessName} — ${appMode==='standalone-pos'?'Terminal PDV':'PDV & ERP'}`;
 const mini=$('#miniBrand');if(mini)mini.textContent=(state.settings.businessName||'G')[0];applyLanguageDOM(document);
}
function hexRgb(hex){const value=String(hex).replace('#','');if(value.length!==6)return'0,152,249';return`${parseInt(value.slice(0,2),16)},${parseInt(value.slice(2,4),16)},${parseInt(value.slice(4,6),16)}`}
const canAdmin=()=>authSession?.role==='admin';
const currentEmployee=()=>state.employees.find(x=>x.id===authSession?.userId)||state.employees.find(x=>x.email===authSession?.email)||(canAdmin()?null:state.employees[0]);
const allowedViews=()=>canAdmin()?['sales','clients','products','stock','catalog','cash','reports','employees','settings']:['sales','clients','products','stock','cash','reports','settings'];

function pageHeader(title,count='',settings=false){const initial=initials(authSession?.name||'Usuario').slice(0,1);return `<header class="page-header"><button class="mobile-menu-button" data-action="toggle-mobile-menu" aria-label="Abrir menú">${svg('menu')}</button><h1 class="page-title">${esc(title)}</h1>${count!==''?`<span class="page-count">(${count})</span>`:''}<span class="header-spacer"></span>${languageSelector(true)}<a class="plan-button" href="./pdv.html" target="_blank">Abrir terminal PDV ${svg('monitor')}</a><button class="user-button" data-action="user-menu" title="${esc(authSession?.name||'Usuario')}">${initial}</button>${settings&&canAdmin()?`<button class="header-settings" data-action="open-quick-settings">${svg('settings')}</button>`:''}</header>`}
function empty(icon,title,text){return `<div class="empty-state"><span class="empty-icon">${svg(icon)}</span><h3>${title}</h3><p>${text}</p></div>`}
function search(value,placeholder,id='pageSearch'){return `<div class="search-field"><span class="search-icon">${svg('search')}</span><input id="${id}" value="${esc(value)}" placeholder="${esc(placeholder)}" autocomplete="off"/><span class="shortcut">Ctrl+F</span></div>`}
function tabs(items,current,attr){return `<div class="tabs">${items.map(([id,label,disabled])=>`<button class="tab ${current===id?'active':''}" data-${attr}="${id}" ${disabled?'disabled':''}>${label}</button>`).join('')}</div>`}

function setView(view){if(!allowedViews().includes(view))view='sales';ui.view=view;ui.search='';ui.menu=false;$('#sidebar')?.classList.remove('open');sessionStorage.setItem('grassi.lastView',view);history.replaceState(null,'',`${location.pathname}#${view}`);$$('.menu-item[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===view));render();$('#app')?.focus({preventScroll:true})}
function render(){if(!authSession)return renderLogin();const map={sales:renderSales,clients:renderClients,products:renderProducts,stock:renderStock,catalog:renderCatalog,cash:renderCash,reports:renderReports,employees:renderEmployees,settings:renderSettings};if(!allowedViews().includes(ui.view))ui.view='sales';$('#app').innerHTML=map[ui.view]();injectIcons($('#app'));syncNavigation();applyLanguageDOM(document)}
function syncNavigation(){const shell=$('.nex-shell');if(shell)shell.hidden=false;$$('.menu-item[data-view]').forEach(button=>{button.hidden=!allowedViews().includes(button.dataset.view);button.classList.toggle('active',button.dataset.view===ui.view)})}
function renderLogin(message=''){
 document.body.classList.add('auth-mode');const shell=$('.nex-shell');if(shell)shell.hidden=false;
 const demo=window.GRASSI_CONFIG?.mode!=='supabase';$('#app').innerHTML=`<section class="login-page"><div class="login-language">${languageSelector()}</div><div class="login-visual"><div class="login-brand"><span>G</span><strong>GRASSI</strong></div><div><p class="eyebrow">PDV + ERP PARA MAYORISTAS</p><h1>Venda rápido.<br>Controle todo.</h1><p>Terminal adaptable, stock en tiempo real, cuentas de clientes y métricas por funcionario.</p></div><small>Moneda principal: boliviano (Bs)</small></div><div class="login-panel"><form id="loginForm" class="login-card"><div class="mobile-logo">G</div><p class="eyebrow">ACCESO SEGURO</p><h2>${appMode==='standalone-pos'?'Abrir terminal PDV':'Iniciar sesión'}</h2><p class="login-copy">Use el acceso asignado por el administrador.</p>${message?`<div class="login-error">${esc(message)}</div>`:''}<div class="field"><label>Correo electrónico</label><input name="email" type="email" autocomplete="username" required placeholder="nombre@empresa.com"></div><div class="field"><label>Contraseña</label><input name="password" type="password" autocomplete="current-password" required placeholder="••••••••"></div><button class="btn primary login-submit" type="submit">${appMode==='standalone-pos'?'Entrar y abrir caja':'Entrar al sistema'}</button>${demo?`<div class="demo-access"><strong>Accesos de demostración</strong><button type="button" data-demo-login="admin">Administrador · admin@example.invalid</button><button type="button" data-demo-login="employee">Funcionario · employee@example.invalid</button><small>Contraseñas: admin123 / func123</small></div>`:''}<span class="login-security">La sesión permanece abierta en este dispositivo hasta cerrar sesión.</span></form></div></section>`;injectIcons($('#app'));applyLanguageDOM(document)
}
async function startAuthenticated(session){authSession=session;document.body.classList.remove('auth-mode');try{const workspace=await GrassiBackend.loadWorkspace();if(workspace){const pending=(state.sales||[]).filter(s=>s.syncStatus==='pending'&&state.offlineQueue.some(q=>q.clientSaleId===s.clientSaleId));state={...state,...workspace,sales:[...pending,...workspace.sales.filter(s=>!pending.some(p=>p.clientSaleId===s.clientSaleId))],settings:{...state.settings,...workspace.settings,appearance:{...state.settings.appearance,...workspace.settings.appearance},posLayout:{...state.settings.posLayout,...workspace.settings.posLayout},cashClosing:{...state.settings.cashClosing,...workspace.settings.cashClosing}}};save()}}catch(error){toast(`No fue posible sincronizar: ${error.message}`,'error')}checkAutomaticCashClose();ensureCashSession();if(appMode==='standalone-pos')openPos();else{const hash=location.hash.slice(1);setView(allowedViews().includes(hash)?hash:ui.view)}setTimeout(showFirstTutorial,250)}
function userMenu(){const head=appMode==='standalone-pos'?`<header class="layer-head"><h1>Mi sesión</h1><button class="layer-close" data-action="back-pos">${svg('close')}</button></header>`:layerHead('Mi sesión');openLayer(`${head}<div class="layer-body"><div class="profile-head">${avatar('',authSession.name)}<div><h3>${esc(authSession.name)}</h3><p>${canAdmin()?'Administrador':'Funcionario'} · ${esc(authSession.email)}</p></div></div><div class="session-actions">${appMode==='standalone-pos'?`<a class="btn primary" href="./index.html#sales">${svg('monitor')} Volver al panel ERP</a>`:''}<button class="btn" data-action="open-help">Ayuda y tutorial</button><button class="btn" data-action="cycle-theme">Cambiar modo visual</button><button class="btn danger" data-action="logout">Cerrar sesión</button></div></div>`,'modal narrow')}
function visibleSales(){if(canAdmin())return state.sales;const employee=currentEmployee();return state.sales.filter(s=>s.employeeId===authSession?.userId||s.employee===employee?.name||s.employee===authSession?.name)}
function byPeriod(rows,label){if(label==='Todo el historial')return rows;if(label==='Este mes'){const now=new Date();return rows.filter(x=>{const d=new Date(x.date);return d.getMonth()===now.getMonth()&&d.getFullYear()===now.getFullYear()})}const days=parseInt(label)||7,limit=Date.now()-days*86400000;return rows.filter(x=>new Date(x.date).getTime()>=limit)}

function renderSales(){
 const tabType={history:'Venta',orders:'Pedido',budgets:'Presupuesto'}[ui.salesTab]||'Venta';
 const rows=byPeriod(visibleSales(),ui.salesPeriod).filter(s=>(s.type||'Venta')===tabType).filter(s=>`${s.id} ${s.client} ${s.employee} ${s.payment}`.toLowerCase().includes(ui.search.toLowerCase())).filter(s=>ui.salesPayment==='Todos'||s.payment===ui.salesPayment);
 return `<section class="page page-sales">${pageHeader('Ventas')}<div class="page-body no-top"><div class="action-bar"><button class="btn primary" data-action="open-pos">Nueva Venta - F3</button><button class="btn secondary" data-action="new-order">Nuevo Pedido</button><button class="btn secondary" data-action="new-budget">Nuevo Presupuesto</button></div>${tabs([['history','Historial'],['orders','Pedidos abiertos'],['accept','Pedidos por aceptar',true],['budgets','Presupuestos']],ui.salesTab,'sales-tab')}<div class="toolbar" style="margin-top:16px">${search(ui.search,'Buscar por número, cliente o vendedor')}<button class="filter-chip" data-action="cycle-sales-payment">${svg('filter')} Pago: ${esc(ui.salesPayment)}</button><button class="filter-chip" data-action="cycle-sales-period">${esc(ui.salesPeriod)} ${svg('down')}</button><span class="grow"></span><span class="filter-chip">Mostrando ${rows.length}</span></div>
 <div class="data-region"><table class="data-table"><thead><tr><th>Acción</th><th>Número</th><th>Resumen</th><th>Tipo</th><th>Fecha</th><th>Hora</th><th>Ítems</th><th>Cliente</th><th>Vendedor</th><th>Subtotal</th><th>Valor pagado</th><th>Pago</th></tr></thead><tbody>${rows.map(s=>`<tr><td><button class="table-action" data-sale-view="${s.id}">Ver</button></td><td>${esc(s.id)}</td><td>${esc(s.items)} productos</td><td>${esc(s.type)}</td><td>${date(s.date)}</td><td>${time(s.date)}</td><td>${s.items}</td><td>${esc(s.client)}</td><td>${esc(s.employee)}</td><td>${money(s.total)}</td><td><strong>${money(s.total)}</strong></td><td>${esc(s.payment)}</td></tr>`).join('')}</tbody></table>${rows.length?'':empty('receipt','Sin transacciones registradas','Sus ventas aparecerán aquí.') }<div class="summary-row"><span>${rows.length} transacciones</span><span>Total <strong>${money(sum(rows,'total'))}</strong></span></div></div></div></section>`
}

function renderClients(){
 const sorter={Nombre:(a,b)=>a.name.localeCompare(b.name),Saldo:(a,b)=>a.balance-b.balance,Compras:(a,b)=>b.purchases-a.purchases}[ui.clientSort];const rows=state.clients.filter(c=>`${c.name} ${c.document} ${c.phone} ${c.code}`.toLowerCase().includes(ui.search.toLowerCase())).filter(c=>ui.clientFilter==='Todos'||ui.clientFilter==='Con deuda'&&c.balance<0||ui.clientFilter==='Con crédito'&&c.balance>0).sort(sorter);
 return `<section class="page page-clients">${pageHeader('Clientes',state.clients.length)}<div class="page-body"><div class="toolbar"><button class="btn circle" data-action="client-new" title="Nuevo cliente">${svg('plus')}</button>${search(ui.search,'Buscar por nombre, documento o teléfono')}<button class="filter-chip" data-action="cycle-client-sort" title="Cambiar orden">${svg('down')} ${esc(ui.clientSort)}</button><button class="filter-chip" data-action="cycle-client-filter">${svg('filter')} ${esc(ui.clientFilter)}</button></div><div class="data-region"><table class="data-table"><thead><tr><th>Acción</th><th>Nombre</th><th>Débito / Crédito</th><th>Estado</th><th>Código</th><th>Observación</th><th>CI / NIT</th><th>Ciudad</th><th>Celular</th><th>Tipo</th></tr></thead><tbody>${rows.map(c=>`<tr><td><button class="table-action" data-client-view="${c.id}">Ver cuenta</button></td><td><div class="item-cell">${avatar(c.photo,c.name)}<div><strong>${esc(c.name)}</strong><small>${esc(c.document)}</small></div></div></td><td class="${c.balance<0?'money-negative':'money-positive'}">${c.balance<0?'− ':c.balance>0?'+ ':''}${money(Math.abs(c.balance))}</td><td><span class="status ${c.balance<0?'danger':''}">${c.balance<0?'Con deuda':c.balance>0?'Con crédito':'Activo'}</span></td><td>${esc(c.code)}</td><td>${esc(c.notes||'—')}</td><td>${esc(c.document)}</td><td>${esc(c.city)}</td><td>${esc(c.phone)}</td><td>${esc(c.type)}</td></tr>`).join('')}</tbody></table>${rows.length?'':empty('users','Ningún cliente encontrado','No hay clientes que correspondan a la búsqueda.')}</div></div></section>`
}

function renderProducts(){
 const cats=['Todas',...new Set(state.products.map(p=>p.category))];
 const sorter={Nombre:(a,b)=>a.name.localeCompare(b.name),Stock:(a,b)=>a.stock-b.stock,Precio:(a,b)=>a.price-b.price}[ui.productSort];const rows=state.products.filter(p=>(ui.productCategory==='Todas'||p.category===ui.productCategory)&&`${p.name} ${p.code} ${p.ean}`.toLowerCase().includes(ui.search.toLowerCase())).filter(p=>ui.productFilter==='Todos'||ui.productFilter==='Stock bajo'&&p.stock<=p.minStock||ui.productFilter==='Sin stock'&&p.stock===0).sort(sorter);
 const toolbar=`<div class="toolbar">${canAdmin()?`<button class="btn circle" data-action="product-new" title="Nuevo producto">${svg('plus')}</button>`:''}${search(ui.search,'Buscar por código, EAN/GTIN o nombre')}<button class="filter-chip" data-action="cycle-product-sort">${svg('down')} ${esc(ui.productSort)}</button><button class="filter-chip" data-action="cycle-product-filter">${svg('filter')} ${esc(ui.productFilter)}</button><button class="btn circle" data-action="open-quick-settings" title="Configuración rápida">${svg('menu')}</button></div>`;
 let content='';
 if(ui.productTab==='labels')content=`${toolbar}<div class="label-action-bar"><div><strong>Etiquetas de precio</strong><span>${rows.length} productos listos para imprimir</span></div><button class="btn primary" data-action="print-labels">${svg('receipt')} Imprimir etiquetas</button></div><div class="product-label-grid">${rows.map(p=>`<article class="product-label"><small>${esc(state.settings.businessName)}</small><strong>${esc(p.name)}</strong><span>${esc(p.code||p.ean||'Sin código')}</span><b>${money(p.price)}</b><em>${esc(p.unit||'Unidad')}</em></article>`).join('')||empty('tag','Sin etiquetas','Registre productos para generar etiquetas de precio.')}</div>`;
 else if(ui.productTab==='suppliers')content=`${toolbar}<div class="data-region"><table class="data-table"><thead><tr><th>Acción</th><th>Producto</th><th>Proveedor</th><th>Código proveedor</th><th>Último costo</th><th>Contacto / nota</th></tr></thead><tbody>${rows.map(p=>`<tr><td>${canAdmin()?`<button class="table-action" data-product-edit="${p.id}">Editar</button>`:'Consultar'}</td><td><strong>${esc(p.name)}</strong><small>${esc(p.code)}</small></td><td>${esc(p.supplier||'Sin proveedor')}</td><td>${esc(p.supplierCode||'—')}</td><td>${money(p.lastCost??p.cost)}</td><td>${esc(p.supplierNote||'—')}</td></tr>`).join('')}</tbody></table>${rows.length?'':empty('store','Sin proveedores','Asigne un proveedor desde la ficha de cada producto.')}</div>`;
 else content=`${toolbar}<div class="products-layout"><aside class="category-panel">${cats.map(c=>`<button class="${ui.productCategory===c?'active':''}" data-category="${esc(c)}">${esc(c)} (${state.products.filter(p=>c==='Todas'||p.category===c).length})</button>`).join('')}${canAdmin()?'<button class="manage" data-action="manage-categories">Gestionar categorías</button>':''}</aside><div class="data-region"><table class="data-table"><thead><tr><th>Acción</th><th>Imagen</th><th>Nombre</th><th>Código</th><th>Precio de venta</th><th>Precio mayorista</th><th>Stock actual</th><th>Costo</th><th>Categoría</th><th>Unidad</th><th>EAN/GTIN</th></tr></thead><tbody>${rows.map(p=>`<tr><td>${canAdmin()?`<button class="table-action" data-product-edit="${p.id}">Editar</button>`:'Consultar'}</td><td><span class="item-thumb">${esc(p.name[0])}</span></td><td><strong>${esc(p.name)}</strong></td><td>${esc(p.code)}</td><td><strong>${money(p.price)}</strong></td><td>${money(p.wholesale)}</td><td><span class="status ${p.stock===0?'danger':p.stock<=p.minStock?'warning':''}">${p.stock}</span></td><td>${money(p.cost)}</td><td>${esc(p.category)}</td><td>${esc(p.unit)}</td><td>${esc(p.ean)}</td></tr>`).join('')}</tbody></table>${rows.length?'':empty('box','Sin productos','Registre su primer producto para comenzar.')}</div></div>`;
 return `<section class="page page-products">${pageHeader('Productos',state.products.length,true)}${tabs([['items','Artículos'],['labels','Etiquetas'],['suppliers','Proveedores']],ui.productTab,'product-tab')}<div class="page-body">${content}</div></section>`
}

function renderStock(){
 const rows=state.products.filter(p=>`${p.name} ${p.code}`.toLowerCase().includes(ui.search.toLowerCase())).filter(p=>ui.stockFilter==='Todos'||ui.stockFilter==='Stock bajo'&&p.stock<=p.minStock||ui.stockFilter==='Sin stock'&&p.stock===0);
 const toolbar=`<div class="toolbar">${search(ui.search,'Buscar por nombre o código del producto')}<button class="filter-chip" data-action="cycle-stock-filter">${svg('filter')} ${esc(ui.stockFilter)}</button><button class="btn circle" data-action="inventory-summary" title="Valor del inventario">${svg('cash')}</button></div>`;
 if(ui.stockTab==='transactions'){const movements=(state.stockMovements||[]).filter(m=>{const p=state.products.find(x=>x.id===m.productId);return`${p?.name||''} ${p?.code||''} ${m.reason||''}`.toLowerCase().includes(ui.search.toLowerCase())});return `<section class="page page-stock">${pageHeader('Inventario')}${tabs([['stock','Stock por producto'],['transactions','Transacciones']],ui.stockTab,'stock-tab')}<div class="page-body">${toolbar}<div class="data-region"><table class="data-table"><thead><tr><th>Fecha</th><th>Producto</th><th>Tipo</th><th>Cantidad</th><th>Motivo</th><th>Funcionario</th></tr></thead><tbody>${movements.map(m=>{const p=state.products.find(x=>x.id===m.productId),label=m.type==='in'?'Entrada':m.type==='out'?'Salida':'Corrección';return`<tr><td>${date(m.date)} · ${time(m.date)}</td><td><strong>${esc(p?.name||'Producto eliminado')}</strong></td><td><span class="status ${m.type==='out'?'danger':m.type==='set'?'warning':''}">${label}</span></td><td class="${m.type==='out'?'money-negative':'money-positive'}">${m.type==='out'?'−':'+'} ${Number(m.amount).toLocaleString(locale())}</td><td>${esc(m.reason||'—')}</td><td>${esc(m.employee||'—')}</td></tr>`}).join('')}</tbody></table>${movements.length?'':empty('box','Sin transacciones','Los movimientos de entrada, salida y corrección aparecerán aquí.')}</div></div></section>`}
 return `<section class="page page-stock">${pageHeader('Inventario')}${tabs([['stock','Stock por producto'],['transactions','Transacciones']],ui.stockTab,'stock-tab')}<div class="page-body">${toolbar}<div class="data-region"><table class="data-table"><thead><tr><th>Acción</th><th>Producto</th><th>Stock actual</th><th>Reservado</th><th>Disponible</th><th>Categoría</th><th>Precio</th><th>Unidad</th><th>Código</th><th>Estado</th></tr></thead><tbody>${rows.map(p=>`<tr><td><button class="table-action" data-stock-move="${p.id}">Movimiento</button></td><td><div class="item-cell"><span class="item-thumb">${esc(p.name[0])}</span><strong>${esc(p.name)}</strong></div></td><td class="stock-number">${p.stock.toFixed(3)}</td><td>0,000</td><td class="stock-number">${p.stock.toFixed(3)}</td><td>${esc(p.category)}</td><td>${money(p.price)}</td><td>${esc(p.unit)}</td><td>${esc(p.code)}</td><td><span class="status ${p.stock===0?'danger':p.stock<=p.minStock?'warning':''}">${p.stock===0?'Sin stock':p.stock<=p.minStock?'Bajo':'Disponible'}</span></td></tr>`).join('')}</tbody></table>${rows.length?'':empty('box','Sin productos','Registre productos para controlar el inventario.')}<div class="summary-row"><span>Valor de costo <strong>${money(sum(state.products.map(p=>({amount:p.stock*p.cost}))))}</strong></span><span>Valor de venta <strong>${money(sum(state.products.map(p=>({amount:p.stock*p.price}))))}</strong></span></div></div></div></section>`
}

function renderCatalog(){return `<section class="page">${pageHeader('Catálogo Online')}<div class="catalog-banner"><div class="catalog-copy"><h2>Su canal de ventas online</h2><p>Publique productos, comparta el catálogo por WhatsApp y reciba pedidos directamente en el sistema. Los precios pueden mostrarse para venta minorista o mayorista.</p><button class="btn primary" data-action="catalog-config">Configurar catálogo</button></div><div class="catalog-preview"><div class="phone-card"><div class="mini-brand">${esc(state.settings.businessName)}</div><div class="mini-products"><span></span><span></span><span></span><span></span><span></span><span></span></div></div></div></div></section>`}

function renderCash(){
 const ins=sum(state.cash.filter(m=>m.type==='in')),outs=sum(state.cash.filter(m=>m.type==='out'));
 const open=state.cashSession?.status==='open',rows=state.cash.filter(m=>ui.cashFilter==='Todos'||ui.cashFilter==='Entradas'&&m.type==='in'||ui.cashFilter==='Salidas'&&m.type==='out');return `<section class="page page-cash">${pageHeader('Caja')}<div class="page-body"><div class="cash-session-banner ${open?'open':'closed'}"><div><span>${open?'Caja abierta':'Caja cerrada'}</span><strong>${open?`Abierta por ${esc(state.cashSession.openedBy||'Usuario')} · ${date(state.cashSession.openedAt)} ${time(state.cashSession.openedAt)}`:'Abra una nueva sesión para registrar movimientos'}</strong></div>${open?`<button class="btn secondary" data-action="cash-close">Cerrar caja</button>`:`<button class="btn primary" data-action="cash-open">Abrir caja</button>`}</div><div class="action-bar"><button class="btn primary" data-action="cash-new" ${open?'':'disabled'}>Nueva transacción</button><span class="grow"></span><button class="filter-chip" data-action="cycle-cash-filter">${svg('filter')} ${esc(ui.cashFilter)}</button></div><div class="metrics"><div class="metric"><span>Saldo inicial</span><strong>${money(0)}</strong></div><div class="metric"><span>Entradas</span><strong class="money-positive">${money(ins)}</strong></div><div class="metric"><span>Salidas</span><strong class="money-negative">${money(outs)}</strong></div><div class="metric"><span>Saldo esperado</span><strong>${money(ins-outs)}</strong></div></div><div class="data-region"><table class="data-table"><thead><tr><th>Acción</th><th>Fecha</th><th>Hora</th><th>Tipo</th><th>Descripción</th><th>Funcionario</th><th>Entrada</th><th>Salida</th></tr></thead><tbody>${rows.map(m=>`<tr><td><button class="table-action" data-cash-view="${m.id}">Ver</button></td><td>${date(m.date)}</td><td>${time(m.date)}</td><td><span class="status ${m.type==='out'?'danger':''}">${m.type==='in'?'Entrada':'Salida'}</span></td><td>${esc(m.description)}</td><td>${esc(m.employee)}</td><td class="money-positive">${m.type==='in'?money(m.amount):'—'}</td><td class="money-negative">${m.type==='out'?money(m.amount):'—'}</td></tr>`).join('')}</tbody></table>${rows.length?'':empty('wallet','Sin movimientos','La caja todavía no tiene entradas ni salidas.')}<div class="summary-row"><span>Movimientos ${rows.length}</span><span>Saldo <strong>${money(ins-outs)}</strong></span></div></div></div></section>`
}

function renderReports(){
 const reports={general:['Resumen','Rentabilidad','Ingresos / gastos','Por producto vendido'],sales:['Medios de pago','Comisión por vendedor','Por producto','Horario pico'],clients:['Ranking de ventas'],payable:['Por vencer','Pagadas']};
 const reportSales=byPeriod(visibleSales(),ui.reportPeriod),reportEmployees=canAdmin()?state.employees:[currentEmployee()].filter(Boolean),total=sum(reportSales,'total'),cost=reportSales.reduce((n,s)=>n+Number(s.cost||0),0),weekdayTotals=[0,0,0,0,0,0,0];reportSales.forEach(s=>{const day=(new Date(s.date).getDay()+6)%7;weekdayTotals[day]+=Number(s.total||0)});const peak=Math.max(...weekdayTotals,0),bars=weekdayTotals.map(value=>peak?Math.max(3,Math.round(value/peak*100)):0);
 return `<section class="page"><div class="report-layout"><aside class="report-menu"><h2>Informes</h2><div class="report-group"><strong>Visión general</strong>${reports.general.map(x=>`<button class="${ui.report===x?'active':''}" data-report="${x}">${x}</button>`).join('')}</div><div class="report-group"><strong>Ventas</strong>${reports.sales.map(x=>`<button class="${ui.report===x?'active':''}" data-report="${x}">${x}</button>`).join('')}</div><div class="report-group"><strong>Clientes</strong>${reports.clients.map(x=>`<button class="${ui.report===x?'active':''}" data-report="${x}">${x}</button>`).join('')}</div><div class="report-group"><strong>Cuentas por pagar</strong>${reports.payable.map(x=>`<button class="${ui.report===x?'active':''}" data-report="${x}">${x}</button>`).join('')}</div></aside><div class="report-content"><div class="report-head"><h2>${esc(ui.report)}</h2><span class="role-chip">${canAdmin()?'Toda la empresa':'Mis ventas'}</span><button class="user-button" data-action="user-menu">${initials(authSession.name).slice(0,1)}</button></div><div class="action-bar"><button class="period-btn" data-action="cycle-report-period">Período: ${esc(ui.reportPeriod)} ${svg('down')}</button><button class="period-btn" data-action="cycle-report-group">${esc(ui.reportGroup)} ${svg('down')}</button><span class="grow"></span><button class="btn secondary" data-action="print-report">${svg('download')} Exportar</button></div><div class="metrics"><div class="metric"><span>Ventas brutas</span><strong>${money(total)}</strong><small>${reportSales.length} transacciones</small></div><div class="metric"><span>Costo estimado</span><strong>${money(cost)}</strong></div><div class="metric"><span>Resultado</span><strong>${money(total-cost)}</strong></div><div class="metric"><span>Ticket promedio</span><strong>${money(reportSales.length?total/reportSales.length:0)}</strong></div></div><div class="chart-panel"><h3>Ventas por ${esc(ui.reportGroup.toLowerCase())}</h3><div class="bars">${bars.map((h,i)=>`<div class="bar" style="height:${h}%"><span>${['Lun','Mar','Mié','Jue','Vie','Sáb','Dom'][i]}</span></div>`).join('')}</div></div><div class="data-region"><table class="data-table"><thead><tr><th>Vendedor</th><th>Ventas</th><th>Facturado</th><th>Ticket promedio</th><th>Meta</th></tr></thead><tbody>${reportEmployees.map(e=>`<tr><td>${esc(e.name)}</td><td>${e.sales}</td><td>${money(e.total)}</td><td>${money(e.ticket)}</td><td><span class="status ${e.goal<70?'warning':''}">${e.goal}%</span></td></tr>`).join('')}</tbody></table></div></div></div></section>`
}

function renderEmployees(){
 const rows=state.employees.filter(e=>`${e.name} ${e.role} ${e.document}`.toLowerCase().includes(ui.search.toLowerCase())).filter(e=>ui.employeeFilter==='Todos'||ui.employeeFilter==='Supervisores'&&e.supervisor||ui.employeeFilter==='Meta baja'&&e.goal<70);
 return `<section class="page page-employees">${pageHeader('Funcionarios',state.employees.length)}<div class="page-body"><div class="toolbar"><button class="btn circle" data-action="employee-new" title="Nuevo funcionario">${svg('plus')}</button>${search(ui.search,'Buscar por nombre, documento o cargo')}<button class="filter-chip" data-action="cycle-employee-filter">${svg('filter')} ${esc(ui.employeeFilter)}</button></div><div class="data-region"><table class="data-table"><thead><tr><th>Acción</th><th>Nombre</th><th>Administrador</th><th>Supervisor</th><th>Cargo</th><th>Ventas</th><th>Facturado</th><th>Ticket promedio</th><th>Meta</th></tr></thead><tbody>${rows.map(e=>`<tr><td><button class="table-action" data-employee-view="${e.id}">Ver perfil</button></td><td><div class="item-cell">${avatar(e.photo,e.name)}<div><strong>${esc(e.name)}</strong><small>${esc(e.email)}</small></div></div></td><td>${e.admin?'Sí':'No'}</td><td>${e.supervisor?'Sí':'No'}</td><td>${esc(e.role)}</td><td>${e.sales}</td><td>${money(e.total)}</td><td>${money(e.ticket)}</td><td><span class="status ${e.goal<70?'warning':''}">${e.goal}%</span></td></tr>`).join('')}</tbody></table></div></div></section>`
}

function renderSettings(){const nav=canAdmin()?[['brand','Empresa y marca'],['appearance','Apariencia'],['pos','Editor de PDV'],['sales','Ventas'],['cash','Caja'],['products','Productos'],['migration','Importar / Exportar']]:[['appearance','Apariencia'],['pos','Editor de PDV']];if(!nav.some(([id])=>id===ui.settingsTab))ui.settingsTab=nav[0][0];return `<section class="page page-settings">${pageHeader('Configuración')}<div class="page-body"><div class="settings-layout"><nav class="settings-nav">${nav.map(([id,label])=>`<button class="${ui.settingsTab===id?'active':''}" data-settings-tab="${id}">${label}</button>`).join('')}</nav><div class="settings-panel">${settingsPanel()}</div></div></div></section>`}
function settingsPanel(){
 const s=state.settings,a=s.appearance||seed.settings.appearance;
 if(ui.settingsTab==='brand')return `<h2>Empresa y marca</h2><p>Identidad white label para cada empresa.</p><form id="brandForm" class="form-grid"><div class="field"><label>Nombre comercial</label><input name="businessName" value="${esc(s.businessName)}" required></div><div class="field"><label>Razón social</label><input name="legalName" value="${esc(s.legalName)}"></div><div class="field"><label>NIT</label><input name="nit" value="${esc(s.nit)}"></div><div class="field"><label>Ciudad</label><input name="city" value="${esc(s.city)}"></div><div class="field"><label>Teléfono</label><input name="phone" value="${esc(s.phone)}"></div><div class="field"><label>Moneda</label><input value="Boliviano (Bs)" disabled></div><div class="field full"><button class="btn primary" type="submit">Guardar cambios</button></div></form>`;
 if(ui.settingsTab==='appearance')return `<h2>Apariencia</h2><p>Elija el diseño completo, el modo y la paleta. La elección se aplica en el sistema y en el terminal PDV.</p><form id="appearanceForm"><div class="choice-label">Diseño del sistema</div><div class="shell-choice-grid"><label class="shell-choice ${a.shell!=='loop'?'active':''}"><input type="radio" name="shell" value="nex" ${a.shell!=='loop'?'checked':''}><span class="shell-preview shell-preview-nex"><i></i><b></b><em></em></span><div><strong>Nex Clásico</strong><small>Menú lateral, visual claro y operacional.</small></div></label><label class="shell-choice ${a.shell==='loop'?'active':''}"><input type="radio" name="shell" value="loop" ${a.shell==='loop'?'checked':''}><span class="shell-preview shell-preview-loop"><i></i><b></b><em></em></span><div><strong>Loop</strong><small>Navegación horizontal, grafite e índigo.</small></div></label></div><div class="choice-label">Modo de pantalla</div><div class="choice-grid">${[['light','Claro'],['dark','Oscuro'],['auto','Automático']].map(([id,label])=>`<label class="choice-card ${a.mode===id?'active':''}"><input type="radio" name="mode" value="${id}" ${a.mode===id?'checked':''}><span class="theme-preview ${id}"></span><strong>${label}</strong></label>`).join('')}</div><div class="choice-label">Paleta principal</div><div class="palette-grid">${Object.entries(palettes).map(([id,color])=>`<label class="palette-option ${id==='grassi'?'grassi-palette':''} ${a.palette===id?'active':''}" style="--swatch:${color}"><input type="radio" name="palette" value="${id}" ${a.palette===id?'checked':''}><span></span>${paletteNames[id]||id}${id==='grassi'?'<i></i>':''}</label>`).join('')}</div><button class="btn primary" type="submit">Aplicar apariencia</button></form>`;
 if(ui.settingsTab==='pos')return posEditor();
 if(ui.settingsTab==='sales')return `<h2>Ventas</h2><p>Parámetros generales de venta e impresión.</p><div class="switch-line"><div><strong>Permitir venta mayorista</strong><div class="page-count">Activa precios especiales en el PDV</div></div><button class="switch ${s.wholesale?'on':''}" data-action="toggle-wholesale-setting" aria-label="Alternar venta mayorista"></button></div><div class="switch-line"><div><strong>Notificación sonora para pedidos</strong><div class="page-count">Avisar al recibir pedidos del catálogo</div></div><button class="switch ${s.options.soundOrders?'on':''}" data-setting-toggle="soundOrders" aria-label="Alternar sonido"></button></div><div class="switch-line"><div><strong>Imprimir recibo después de la venta</strong></div><button class="switch ${s.options.printReceipt?'on':''}" data-setting-toggle="printReceipt" aria-label="Alternar impresión"></button></div>`;
 if(ui.settingsTab==='cash'){const cfg=s.cashClosing||seed.settings.cashClosing;return `<h2>Cierre de caja</h2><p>La pestaña o el navegador nunca cierran la caja. Configure un horario si desea cierre automático.</p><form id="cashClosingForm" class="form-grid"><div class="field"><label>Cierre automático</label><select name="automatic"><option value="false" ${!cfg.automatic?'selected':''}>Desactivado</option><option value="true" ${cfg.automatic?'selected':''}>Activado</option></select></div><div class="field"><label>Hora diaria de cierre</label><input type="time" name="time" value="${esc(cfg.time||'00:00')}" required></div><div class="field full"><div class="form-warning">Ejemplo: 00:00 cierra la sesión abierta al cambiar el día. El sistema evita cierres duplicados.</div></div><div class="field full"><button class="btn primary">Guardar horario</button></div></form>`}
 if(ui.settingsTab==='products'){const opts=[['autoCode','Generar códigos automáticos con 6 dígitos'],['quickProduct','Usar registro rápido de productos'],['duplicateCode','Permitir productos con el mismo código'],['blockNoStock','No permitir venta sin stock'],['autoPrice','Calcular precio de venta automáticamente']];return `<h2>Productos</h2><p>Reglas para códigos, stock y precios.</p>${opts.map(([key,label])=>`<div class="switch-line"><strong>${label}</strong><button class="switch ${s.options[key]?'on':''}" data-setting-toggle="${key}" aria-label="Alternar ${esc(label)}"></button></div>`).join('')}`}
 return `<h2>Importar / Exportar</h2><p>Migre productos, funcionarios, clientes, crediarios y movimientos.</p><div class="migration-grid"><div class="migration-item"><h3>Respaldo completo</h3><p>Todos los módulos y configuraciones en JSON.</p><button class="btn small" data-action="export-all">Exportar</button><button class="btn small" data-action="import-backup">Importar</button></div><div class="migration-item"><h3>Productos</h3><p>Códigos, precios, costo, inventario y categorías.</p><button class="btn small" data-action="export-products">Exportar</button><button class="btn small" data-action="import-products">Importar</button></div><div class="migration-item"><h3>Clientes y crediarios</h3><p>Datos, saldo y movimientos de crédito/débito.</p><button class="btn small" data-action="export-clients">Exportar</button><button class="btn small" data-action="import-clients">Importar</button></div><div class="migration-item"><h3>Funcionarios</h3><p>Perfiles, permisos y métricas individuales.</p><button class="btn small" data-action="export-employees">Exportar</button><button class="btn small" data-action="import-employees">Importar</button></div></div>`
}
const widgetInfo={client:['Cliente','users'],wholesale:['Precio mayorista','store'],delivery:['Entrega','transfer'],notes:['Observaciones','edit'],payment:['Finalizar compra','cash']};
const posShortcuts=[['F1','Atajos','help'],['F2','Buscar producto','search'],['F4','Elegir cliente','users'],['F6','Precio mayorista','store'],['F8','Cantidad','box'],['F9','Finalizar compra','cash'],['Enter','Añadir resultado','plus'],['Supr','Quitar último','trash']];
const touchShortcuts=posShortcuts.filter(([key])=>['F2','F4','F6','F8','F9','Supr'].includes(key));
function posEditor(){const layout=state.settings.posLayout||seed.settings.posLayout;return `<h2>Editor de PDV</h2><p>Arrastre los controles, elija el tema y defina cómo el terminal aparecerá en computadora o celular.</p><form id="posLayoutForm"><div class="editor-options"><label>Tema del PDV<select name="theme"><option value="touch" ${layout.theme==='touch'?'selected':''}>Touch moderno</option><option value="classic" ${layout.theme==='classic'?'selected':''}>Clásico claro</option><option value="grassi" ${layout.theme==='grassi'?'selected':''}>GRASSI Racing</option><option value="contrast" ${layout.theme==='contrast'?'selected':''}>Alto contraste</option></select></label><label>Zona de controles<select name="dock"><option value="sidebar" ${layout.dock==='sidebar'?'selected':''}>Lateral</option><option value="footer" ${layout.dock==='footer'?'selected':''}>Inferior</option></select></label><label>Densidad<select name="density"><option value="comfortable" ${layout.density==='comfortable'?'selected':''}>Cómoda</option><option value="compact" ${layout.density==='compact'?'selected':''}>Compacta</option></select></label></div><div class="pos-theme-preview theme-${esc(layout.theme||'touch')}"><span></span><i></i><b></b><small>${esc(state.settings.businessName)} PDV</small></div><div class="pos-editor-list" id="posEditorList">${layout.items.map((id,index)=>`<div class="pos-editor-item" draggable="true" data-widget="${id}"><span class="drag-handle">⋮⋮</span>${svg(widgetInfo[id]?.[1]||'box')}<strong>${widgetInfo[id]?.[0]||id}</strong><span>${id==='payment'?'Obligatorio':`Posición ${index+1}`}</span></div>`).join('')}</div><div class="editor-actions"><button type="button" class="btn" data-action="preview-pos">Vista previa</button><button class="btn primary" type="submit">Guardar layout</button></div></form>`}

function openLayer(html,kind=''){const l=$('#layer');l.hidden=false;$('#layerContent').innerHTML=`<section class="layer-card ${kind}">${html}</section>`;injectIcons(l);document.body.style.overflow='hidden';applyLanguageDOM(l)}
function closeLayer(){const l=$('#layer');l.hidden=true;$('#layerContent').innerHTML='';document.body.style.overflow=''}
const layerHead=(title,close=true)=>`<header class="layer-head"><h1>${esc(title)}</h1>${close?`<button class="layer-close" data-action="close-layer">${svg('close')}</button>`:''}</header>`;

const supportPhone='5519989765398';
function supportWhatsAppUrl(data){const module=ui.view==='settings'?'Configuración':ui.view||'Sistema',lines=[`Hola, necesito ayuda con ${state.settings.businessName||'GRASSI PDV'}.`,`Nombre: ${data.name||authSession?.name||'No informado'}`,`Categoría: ${data.category||'Soporte'}`,`Asunto: ${data.subject||'Sin asunto'}`,`Módulo: ${module}`,'',data.description||''];return`https://wa.me/${supportPhone}?text=${encodeURIComponent(lines.join('\n'))}`}
function supportForm(){openLayer(`${layerHead('Centro de ayuda')}<form id="supportForm"><div class="layer-body support-form"><div class="support-intro"><span>${svg('help')}</span><div><strong>Ayuda y soporte</strong><p>Cuéntenos qué necesita antes de continuar a WhatsApp.</p></div></div><div class="form-grid"><div class="field"><label>Nombre</label><input name="name" value="${esc(authSession?.name||'')}" required></div><div class="field"><label>Categoría</label><select name="category" required><option>Problema técnico</option><option>Duda de uso</option><option>Solicitud / requisito</option><option>Financiero</option><option>Otro</option></select></div><div class="field full"><label>Asunto</label><input name="subject" maxlength="100" placeholder="Resumen breve del problema" required></div><div class="field full"><label>Descripción detallada</label><textarea name="description" rows="6" minlength="10" placeholder="Explique qué intentó hacer, qué ocurrió y qué necesita..." required></textarea></div></div><div class="support-note">${svg('transfer')}<span>El mensaje se abrirá listo para revisar. Usted decide cuándo enviarlo.</span></div></div><footer class="layer-foot"><button type="button" class="btn" data-action="close-layer">Cancelar</button><button type="submit" class="btn primary support-whatsapp">Continuar en WhatsApp</button></footer></form>`,'modal narrow')}
function helpCenter(){openLayer(`${layerHead('Ayuda y soporte')}<div class="layer-body help-center"><button data-action="start-tutorial"><span>${svg('monitor')}</span><div><strong>Ver tutorial del sistema</strong><small>Repase el flujo del ERP, PDV, clientes, caja e informes.</small></div><b>›</b></button><button data-action="open-support"><span>${svg('help')}</span><div><strong>Contactar por WhatsApp</strong><small>Describa el problema o requisito antes de abrir el mensaje.</small></div><b>›</b></button></div><footer class="layer-foot"><button class="btn primary" data-action="close-layer">Listo</button></footer>`,'modal narrow')}
const tutorialKey=()=>`grassi.tutorial.v1.${authSession?.userId||authSession?.email||'user'}.${appMode}`;
function tutorialSteps(){return appMode==='standalone-pos'?[['Su terminal de venta','Busque o toque productos, cambie cantidades y use los atajos visibles en la parte inferior.','cart'],['Venta rápida','El panel lateral reúne cliente, precio mayorista, entrega y observaciones. Puede ocultarlo por completo en computadora.','monitor'],['Cobro guiado','Finalizar compra abre revisión, forma de pago y cupón. La venta solo se registra al confirmar el pago.','cash'],['Volver al ERP','Abra “Mi sesión” en la esquina superior y use “Volver al panel ERP”. Cerrar la pestaña no cierra la caja.','chart']]:[['Panel ERP',canAdmin()?'Use el menú para acceder a ventas, clientes, productos, inventario, caja, informes y funcionarios.':'Use el menú para acceder a ventas, clientes, productos, inventario, caja e informes.','monitor'],['Clientes y cuentas','Cada cliente tiene ficha, foto, datos, cuenta corriente y movimientos de débito, crédito y pago.','users'],['PDV','Abra el terminal con el botón superior. El flujo es producto → revisión → pago → cupón, sin recargar la página.','cart'],['Caja e informes','La caja solo se cierra con el botón “Cerrar caja” o en el horario automático. Los informes usan únicamente registros reales.','chart'],['Personalización y ayuda','En Configuración elija tema, paleta y layout del PDV. En Ayuda puede repetir este tutorial o contactar soporte.','settings']];}
function renderTutorial(){const steps=tutorialSteps(),step=steps[ui.tutorialStep]||steps[0],last=ui.tutorialStep===steps.length-1;openLayer(`<header class="tutorial-head"><span>${ui.tutorialStep+1} / ${steps.length}</span><button data-action="skip-tutorial">Saltar tutorial</button></header><div class="tutorial-body"><div class="tutorial-icon">${svg(step[2])}</div><p class="eyebrow">PRIMEROS PASOS</p><h1>${esc(step[0])}</h1><p>${esc(step[1])}</p><div class="tutorial-progress">${steps.map((_,i)=>`<i class="${i<=ui.tutorialStep?'active':''}"></i>`).join('')}</div></div><footer class="layer-foot"><button class="btn" data-action="tutorial-back" ${ui.tutorialStep===0?'disabled':''}>Anterior</button><button class="btn primary" data-action="${last?'finish-tutorial':'tutorial-next'}">${last?'Comenzar':'Siguiente'}</button></footer>`,'modal narrow tutorial-modal')}
function startTutorial(){ui.tutorialStep=0;renderTutorial()}
function finishTutorial(){localStorage.setItem(tutorialKey(),'1');closeLayer();toast('Tutorial concluido. Puede verlo nuevamente en Ayuda y soporte.')}
function showFirstTutorial(){if(!localStorage.getItem(tutorialKey()))startTutorial()}

function ensureCashSession(){if(state.cashSession?.status==='open')return state.cashSession;state.cashSession={id:`CX${Date.now()}`,status:'open',openedAt:new Date().toISOString(),openedBy:authSession?.name||'Usuario'};save();return state.cashSession}
function closeCash(reason='manual'){const session=state.cashSession;if(!session||session.status!=='open')return toast('La caja ya está cerrada','error');const opened=new Date(session.openedAt).getTime(),movements=state.cash.filter(m=>new Date(m.date).getTime()>=opened),ins=sum(movements.filter(m=>m.type==='in')),outs=sum(movements.filter(m=>m.type==='out')),closing={id:`FC${Date.now()}`,date:new Date().toISOString(),openedAt:session.openedAt,total:ins-outs,entries:ins,exits:outs,reason,employee:authSession?.name||'Sistema'};state.closings.push(closing);state.cashSession={...session,status:'closed',closedAt:closing.date,closedBy:closing.employee,closingId:closing.id};save();if(ui.view==='cash'&&appMode!=='standalone-pos')render();toast(reason==='automatic'?'Caja cerrada automáticamente':'Caja cerrada correctamente');return closing}
function checkAutomaticCashClose(){const cfg=state.settings.cashClosing;if(!cfg?.automatic||state.cashSession?.status!=='open')return;const [hour,minute]=String(cfg.time||'00:00').split(':').map(Number),now=new Date(),threshold=new Date(now);threshold.setHours(hour||0,minute||0,0,0);if(now<threshold)threshold.setDate(threshold.getDate()-1);if(new Date(state.cashSession.openedAt)<=threshold)closeCash('automatic')}

function openPos(reset=true,mode='Venta'){if(reset){ui.saleMode=mode;ui.cart=[];ui.posSearch='';ui.posCategory='Todos';ui.posMobileView='products';ui.posQty=1;ui.posClient='';ui.wholesale=false;ui.posNotes='';ui.posDelivery='Retiro en tienda'}renderPosLayer()}
const posItemTotal=item=>Number(item.qty||0)*Number(item.price||0);
const posSubtotal=()=>ui.cart.reduce((n,i)=>n+Number(i.qty||0)*Number(i.basePrice??i.price),0);
const posTotal=()=>ui.cart.reduce((n,i)=>n+posItemTotal(i),0);
const queueStatus=()=>!navigator.onLine?['Sin conexión','offline']:state.offlineQueue.length?[`${state.offlineQueue.length} pendiente${state.offlineQueue.length===1?'':'s'}`,'pending']:[GrassiBackend.isRemote()?'En línea':'Modo local','online'];
function posWidget(id,place='sidebar'){
 const compact=place==='footer',client=state.clients.find(c=>c.id===ui.posClient),info=widgetInfo[id]||[id,'box'];
 const config={client:{action:'pos-client',label:client?.name||'Cliente'},wholesale:{action:'toggle-wholesale',label:ui.wholesale?'Mayorista activo':'Precio mayorista'},delivery:{action:'pos-delivery',label:ui.posDelivery||'Entrega'},notes:{action:'pos-notes',label:ui.posNotes?'Observación lista':'Observaciones'},payment:{action:'review-sale',label:'Finalizar compra'}}[id];
 return `<button class="pos-widget ${id==='payment'?'primary-widget':''}" data-action="${config.action}" data-pos-widget="${id}" ${id==='payment'&&!ui.cart.length?'disabled':''}>${svg(info[1])}<span data-pos-widget-label>${esc(config.label)}</span>${compact?'':`<span>›</span>`}</button>`
}
function posResults(){return ui.posSearch?state.products.filter(p=>`${p.name} ${p.code} ${p.ean}`.toLowerCase().includes(ui.posSearch.toLowerCase())).slice(0,7):[]}
function posSuggestionsHtml(rows=posResults()){return rows.map(p=>`<button class="suggestion" data-pos-add="${p.id}"><span class="item-thumb">${p.name[0]}</span><span><strong>${esc(p.name)}</strong><small>${esc(p.code)} · Stock ${p.stock}</small></span><b>${money(ui.wholesale?p.wholesale:p.price)}</b></button>`).join('')}
function posCatalogProducts(){const term=ui.posSearch.trim().toLowerCase();return state.products.filter(p=>(ui.posCategory==='Todos'||(p.category||'Sin categoría')===ui.posCategory)&&(!term||`${p.name} ${p.code} ${p.ean} ${p.category}`.toLowerCase().includes(term))).slice(0,80)}
function posCatalogHtml(){
 const categories=['Todos',...new Set(state.products.map(p=>p.category||'Sin categoría'))],rows=posCatalogProducts();
 return `<section class="pos-catalog"><header class="pos-catalog-head"><div><strong>Catálogo de productos</strong><small>Productos visibles para venta rápida</small></div><b>${rows.length}</b></header><div class="pos-category-strip">${categories.map(category=>`<button class="${ui.posCategory===category?'active':''}" data-pos-category="${esc(category)}">${category==='Todos'?'Todos los productos':esc(category)}</button>`).join('')}</div><div class="pos-product-grid">${rows.map(p=>{const blocked=state.settings.options.blockNoStock&&Number(p.stock)<=0,price=ui.wholesale?Number(p.wholesale||p.price):Number(p.price);return `<button class="pos-product-card ${blocked?'out':''}" data-pos-add="${p.id}" ${blocked?'disabled':''}><span class="pos-product-visual">${esc((p.name||'?')[0].toUpperCase())}<small>${esc(p.category||'Sin categoría')}</small></span><span class="pos-product-name">${esc(p.name)}</span><span class="pos-product-meta"><small>${esc(p.code||p.ean||'Sin código')}</small><em>Stock ${Number(p.stock||0).toLocaleString(locale())}</em></span><strong>${money(price)}</strong><i>${blocked?'Sin stock':'Toque para añadir'}</i></button>`}).join('')||`<div class="pos-catalog-empty">${svg('box')}<strong>${state.products.length?'No hay productos en este filtro':'El inventario está vacío'}</strong><span>${state.products.length?'Elija otra categoría o cambie la búsqueda.':'Registre productos desde el panel administrativo para mostrarlos aquí.'}</span></div>`}</div></section>`
}
function posCartHtml(){if(!ui.cart.length)return`<div class="cart-empty"><div>${svg('cart')}Ningún artículo añadido<small>${state.products.length?'Toque un producto del catálogo para comenzar':'El inventario está vacío. Registre sus productos desde el panel administrativo.'}</small></div></div>`;const rows=ui.cart.map((i,n)=>`<tr><td>${n+1}</td><td><button class="cart-product" data-cart-edit="${i.id}"><strong>${esc(i.name)}</strong><small>${esc(i.code)} · Toque para editar</small></button></td><td><div class="qty-stepper"><button data-cart-dec="${i.id}" aria-label="Disminuir cantidad">−</button><input data-cart-qty="${i.id}" type="number" min="1" value="${i.qty}" aria-label="Cantidad de ${esc(i.name)}"><button data-cart-inc="${i.id}" aria-label="Aumentar cantidad">+</button></div></td><td>${money(i.price)}</td><td>${Number(i.discount||0).toLocaleString(locale())}%</td><td><strong>${money(posItemTotal(i))}</strong></td><td><button class="table-action icon" data-pos-remove="${i.id}" aria-label="Quitar ${esc(i.name)}">${svg('close')}</button></td></tr>`).join('');const cards=ui.cart.map(i=>`<article class="pos-cart-card"><header><div><strong>${esc(i.name)}</strong><small>${esc(i.code)}</small></div><button data-pos-remove="${i.id}" aria-label="Quitar ${esc(i.name)}">${svg('trash')}</button></header><div class="pos-cart-card-body"><div class="qty-stepper"><button data-cart-dec="${i.id}" aria-label="Disminuir cantidad">−</button><input data-cart-qty="${i.id}" type="number" min="1" value="${i.qty}" aria-label="Cantidad de ${esc(i.name)}"><button data-cart-inc="${i.id}" aria-label="Aumentar cantidad">+</button></div><button class="pos-cart-mobile-edit" data-cart-edit="${i.id}">${svg('edit')} Precio y descuento</button></div><footer><span>${money(i.price)} × ${i.qty}${i.discount?` · −${Number(i.discount).toLocaleString(locale())}%`:''}</span><strong>${money(posItemTotal(i))}</strong></footer></article>`).join('');return`<div class="pos-cart-desktop"><table class="data-table pos-cart-table"><thead><tr><th>Nº</th><th>Producto</th><th>Cant.</th><th>Precio</th><th>Descuento</th><th>Total</th><th></th></tr></thead><tbody>${rows}</tbody></table></div><div class="pos-cart-mobile">${cards}</div>`}
function refreshPosSearch(){const box=$('#posSuggestions'),catalog=$('#posCatalogRegion');if(box){const rows=posResults();box.innerHTML=posSuggestionsHtml(rows);box.hidden=!rows.length;injectIcons(box)}if(catalog){catalog.innerHTML=posCatalogHtml();injectIcons(catalog);applyLanguageDOM(catalog)}}
function refreshPosView(){const region=$('#posCartRegion');if(!region)return renderPosLayer();const total=posTotal(),subtotal=posSubtotal(),qty=ui.cart.reduce((n,i)=>n+i.qty,0);region.innerHTML=posCartHtml();injectIcons(region);if($('#posCartTitle'))$('#posCartTitle').textContent=ui.cart.length?`${qty} ${qty===1?'artículo':'artículos'}`:'Ningún artículo';if($('#posCartQuantity'))$('#posCartQuantity').textContent=`Cantidad: ${qty}`;if($('#posMobileCartCount'))$('#posMobileCartCount').textContent=qty;if($('#posSubtotal'))$('#posSubtotal').textContent=money(subtotal);if($('#posDiscount'))$('#posDiscount').textContent=`− ${money(Math.max(0,subtotal-total))}`;if($('#posTotal'))$('#posTotal').textContent=money(total);if($('#posCollapsedTotal'))$('#posCollapsedTotal').textContent=money(total);if($('#posCollapsedCount'))$('#posCollapsedCount').textContent=`${qty} ${qty===1?'item':'itens'}`;$$('[data-pos-widget="payment"]').forEach(x=>x.disabled=!ui.cart.length);$$('.pos-touch-shortcuts .finish').forEach(x=>x.disabled=!ui.cart.length);$$('[data-pos-widget="wholesale"] [data-pos-widget-label]').forEach(x=>x.textContent=ui.wholesale?'Mayorista activo':'Precio mayorista');refreshPosSearch();applyLanguageDOM(region)}
function renderPosLayer(){
 const total=posTotal(),subtotal=posSubtotal(),qty=ui.cart.reduce((n,i)=>n+i.qty,0),results=posResults(),layout=state.settings.posLayout||seed.settings.posLayout,widgets=layout.items||seed.settings.posLayout.items,employee=currentEmployee(),status=queueStatus();
 const sideWidgets=layout.dock==='sidebar'?widgets.map(id=>`<li>${posWidget(id)}</li>`).join(''):'';
 const footerWidgets=layout.dock==='footer'?`<div class="pdv-custom-dock">${widgets.map(id=>posWidget(id,'footer')).join('')}</div>`:'';
 const html=`<div class="pdv density-${layout.density||'comfortable'} dock-${layout.dock||'sidebar'} pos-theme-${layout.theme||'touch'} mobile-view-${ui.posMobileView||'products'} ${ui.posSidebarCollapsed?'sidebar-collapsed':''}"><section class="pdv-main"><div class="pdv-title"><div><span class="terminal-label">${esc(state.settings.businessName)} · TERMINAL</span><h1>Venta</h1></div><div class="pdv-head-actions">${languageSelector(true)}<button class="shortcut-trigger" data-action="pos-shortcuts"><kbd>F1</kbd> Atajos</button><span class="sync-pill ${status[1]}" id="syncPill">● ${esc(status[0])}</span><button class="layer-close" data-action="${appMode==='standalone-pos'?'user-menu':'close-layer'}" aria-label="Sesión">${svg(appMode==='standalone-pos'?'users':'close')}</button></div></div><div class="pdv-search">${search(ui.posSearch,'Código, nombre o use el lector de código de barras','posSearch')}<label class="quantity-field">Cant.: <input id="posQty" type="number" min="1" value="${ui.posQty}"></label><div class="suggestions" id="posSuggestions" ${results.length?'':'hidden'}>${posSuggestionsHtml(results)}</div></div><div class="pos-mobile-tools">${['client','wholesale','delivery','notes'].map(id=>posWidget(id)).join('')}</div><nav class="pos-mobile-view-switch"><button class="${ui.posMobileView!=='cart'?'active':''}" data-pos-mobile-view="products">${svg('box')} Productos</button><button class="${ui.posMobileView==='cart'?'active':''}" data-pos-mobile-view="cart">${svg('cart')} Carrito <b id="posMobileCartCount">${qty}</b></button></nav><div class="pdv-table-wrap"><div class="pos-sale-workspace"><div id="posCatalogRegion">${posCatalogHtml()}</div><section class="pos-cart-panel"><header><div><strong>Carrito de venta</strong><small id="posCartQuantity">Cantidad: ${qty}</small></div><b id="posCartTitle">${ui.cart.length?`${qty} ${qty===1?'artículo':'artículos'}`:'Ningún artículo'}</b></header><div class="data-region" id="posCartRegion">${posCartHtml()}</div></section></div></div><footer class="pdv-footer">${footerWidgets}<div class="pdv-totals"><span>Subtotal <b id="posSubtotal">${money(subtotal)}</b></span><span>Descuento <b id="posDiscount">− ${money(Math.max(0,subtotal-total))}</b></span></div><div class="pdv-total"><span>Total</span><strong id="posTotal">${money(total)}</strong></div></footer></section><aside class="pdv-sidebar"><button class="pos-panel-toggle" data-action="toggle-pos-panel" title="Minimizar panel" aria-label="Minimizar panel">${svg('close')}<span>Minimizar</span></button><div class="pos-side-content"><div class="pdv-side-head"><h2>${ui.cart.length?`${qty} ${qty===1?'artículo':'artículos'}`:'Venta rápida'}</h2><span>Panel interactivo</span></div><ul class="pdv-links">${sideWidgets}</ul><div class="pdv-seller"><strong>Hecho por ${esc(employee?.name||authSession?.name||'Usuario')}</strong>${new Intl.DateTimeFormat(locale(),{dateStyle:'short',timeStyle:'short'}).format(new Date())}</div></div></aside>${ui.posSidebarCollapsed?`<button class="pos-panel-reopen" data-action="toggle-pos-panel" title="Abrir Venta rápida" aria-label="Abrir Venta rápida">${svg('cart')}<span>Venta rápida</span><strong id="posCollapsedTotal">${money(total)}</strong></button>`:''}<nav class="pos-touch-shortcuts" aria-label="Atajos rápidos">${touchShortcuts.map(([key,label,icon])=>`<button data-pos-command="${key}" class="${key==='F9'?'finish':''}" ${key==='F9'&&!ui.cart.length?'disabled':''}>${svg(icon)}<span>${esc(label)}</span><kbd>${key}</kbd></button>`).join('')}</nav></div>`;
 if(appMode==='standalone-pos'){closeLayer();$('#app').innerHTML=html;document.body.style.overflow='';injectIcons($('#app'));applyLanguageDOM($('#app'))}else openLayer(html);
 const posTitle=$('.pdv-title h1');if(posTitle)posTitle.textContent=ui.saleMode||'Venta';
 setTimeout(()=>$('#posSearch')?.focus(),0)
}

const saleSteps=active=>`<div class="sale-steps">${[['1','Revisar'],['2','Pago'],['3','Cupón']].map(([n,label])=>`<span class="${Number(n)<=active?'active':''}"><b>${n}</b>${label}</span>`).join('')}</div>`;
function reviewSaleLayer(){if(!ui.cart.length)return toast('Añada al menos un producto','error');const client=state.clients.find(c=>c.id===ui.posClient);openLayer(`${layerHead('Finalizar compra')}${saleSteps(1)}<div class="layer-body sale-review"><div class="detail-grid"><div><span>Productos</span><strong>${ui.cart.reduce((n,i)=>n+i.qty,0)}</strong></div><div><span>Cliente</span><strong>${esc(client?.name||'Consumidor final')}</strong></div><div><span>Entrega</span><strong>${esc(ui.posDelivery||'Retiro en tienda')}</strong></div><div><span>Observaciones</span><strong>${esc(ui.posNotes||'Sin observaciones')}</strong></div></div><div class="payment-total"><span>Total de la compra</span><strong>${money(posTotal())}</strong></div></div><footer class="layer-foot"><button type="button" class="btn" data-action="back-pos">Volver</button><button type="button" class="btn primary" data-action="payment">Continuar al pago</button></footer>`,'modal narrow')}
function paymentLayer(){const total=posTotal();openLayer(`${layerHead('Forma de pago')}${saleSteps(2)}<form id="paymentForm"><div class="payment-modal"><div class="payment-total"><span>Total a cobrar</span><strong>${money(total)}</strong></div><div class="payment-options">${[['Efectivo','cash'],['PIX','receipt'],['QR','qr'],['Transferencia','transfer'],['Cuenta cliente','users']].map(([n,i])=>`<button type="button" class="payment-option ${ui.payment===n?'active':''}" data-pay="${n}" ${n==='Cuenta cliente'&&!ui.posClient?'disabled':''}>${svg(i)}<strong>${n}</strong></button>`).join('')}</div><div class="field" style="margin-top:18px"><label>Valor recibido</label><input name="received" type="number" step="0.01" min="0" value="${total.toFixed(2)}"></div></div><footer class="layer-foot"><button type="button" class="btn" data-action="review-sale">Volver</button><button type="submit" class="btn primary">Confirmar pago</button></footer></form>`,'modal narrow')}
function posClientPicker(){openLayer(`${layerHead('Seleccionar cliente')}<div class="layer-body"><div class="data-region"><table class="data-table"><thead><tr><th>Nombre</th><th>Tipo</th><th>Saldo</th><th></th></tr></thead><tbody>${state.clients.map(c=>`<tr><td>${esc(c.name)}</td><td>${c.type}</td><td>${c.balance<0?'− ':c.balance>0?'+ ':''}${money(Math.abs(c.balance))}</td><td><button class="table-action" data-select-client="${c.id}">Seleccionar</button></td></tr>`).join('')}</tbody></table></div></div><footer class="layer-foot"><button class="btn" data-action="back-pos">Volver</button></footer>`,'modal')}
function posNotesForm(){openLayer(`${layerHead('Observaciones')}<form id="posNotesForm"><div class="layer-body"><div class="field"><label>Observación de la venta</label><textarea name="notes" rows="5" placeholder="Ej.: entregar después de las 16:00">${esc(ui.posNotes)}</textarea></div></div><footer class="layer-foot"><button type="button" class="btn" data-action="back-pos">Cancelar</button><button class="btn primary">Guardar observación</button></footer></form>`,'modal narrow')}
function posDeliveryForm(){openLayer(`${layerHead('Entrega')}<form id="posDeliveryForm"><div class="layer-body"><div class="field"><label>Modalidad</label><select name="delivery"><option ${ui.posDelivery==='Retiro en tienda'?'selected':''}>Retiro en tienda</option><option ${ui.posDelivery==='Entrega local'?'selected':''}>Entrega local</option><option ${ui.posDelivery==='Envío por transporte'?'selected':''}>Envío por transporte</option></select></div><div class="field"><label>Referencia</label><input name="reference" value="" placeholder="Dirección, transportadora o contacto"></div></div><footer class="layer-foot"><button type="button" class="btn" data-action="back-pos">Cancelar</button><button class="btn primary">Guardar entrega</button></footer></form>`,'modal narrow')}
function cartItemForm(id){const item=ui.cart.find(i=>i.id===id);if(!item)return;openLayer(`${layerHead('Editar producto')}<form id="cartItemForm" data-id="${item.id}"><div class="layer-body"><div class="movement-client"><span class="item-thumb">${esc(item.name[0])}</span><div><strong>${esc(item.name)}</strong><small>${esc(item.code)}</small></div></div><div class="form-grid"><div class="field"><label>Cantidad</label><input name="qty" type="number" min="1" step="1" value="${item.qty}" required></div><div class="field"><label>Precio unitario</label><input name="price" type="number" min="0" step="0.01" value="${Number(item.basePrice??item.price).toFixed(2)}" required></div><div class="field full"><label>Descuento (%)</label><input name="discount" type="number" min="0" max="100" step="0.01" value="${Number(item.discount||0)}"></div></div></div><footer class="layer-foot"><button type="button" class="btn" data-action="back-pos">Cancelar</button><button class="btn primary">Aplicar</button></footer></form>`,'modal narrow')}
function toggleWholesalePos(){ui.wholesale=!ui.wholesale;ui.cart=ui.cart.map(i=>{const p=state.products.find(x=>x.id===i.id),base=ui.wholesale?Number(p?.wholesale||p?.price||i.basePrice):Number(p?.price||i.basePrice);return{...i,basePrice:base,price:base*(1-Number(i.discount||0)/100)}});refreshPosView();toast(ui.wholesale?'Precio mayorista activado':'Precio minorista activado')}
function shortcutPanel(){openLayer(`${layerHead('Atajos del terminal')}<div class="shortcut-panel"><p>Use el teclado o toque una acción. Los atajos funcionan mientras el terminal está abierto.</p><div class="shortcut-grid">${posShortcuts.map(([key,label,icon])=>`<button data-pos-command="${key}">${svg(icon)}<span><strong>${label}</strong><small>${key}</small></span></button>`).join('')}</div><div class="shortcut-tip"><kbd>Ctrl</kbd><kbd>↑</kbd><kbd>↓</kbd><span>Cambia la cantidad del último producto sin salir de la venta.</span></div></div><footer class="layer-foot"><button class="btn primary" data-action="back-pos">Volver al PDV</button></footer>`,'modal narrow')}
function runPosCommand(command){if(command==='F1')return shortcutPanel();if(command==='F2'||command==='search'){if(!$('#posSearch'))renderPosLayer();return setTimeout(()=>$('#posSearch')?.focus(),0)}if(command==='F4')return posClientPicker();if(command==='F6'){if(!$('.pdv'))renderPosLayer();return setTimeout(toggleWholesalePos,0)}if(command==='F8'){if(!$('#posQty'))renderPosLayer();return setTimeout(()=>{$('#posQty')?.focus();$('#posQty')?.select()},0)}if(command==='F9'){if(!ui.cart.length)return toast('Añada al menos un producto','error');return reviewSaleLayer()}if(command==='Enter'){if(!ui.posSearch.trim())return toast('Escriba un código o nombre primero','error');const match=state.products.find(p=>`${p.name} ${p.code} ${p.ean}`.toLowerCase().includes(ui.posSearch.toLowerCase())&&(!state.settings.options.blockNoStock||p.stock>0));return match?addCart(match.id):toast('No hay un producto disponible para añadir','error')}if(command==='Supr'){if(!ui.cart.length)return toast('La venta está vacía','error');ui.cart.pop();return refreshPosView()}}

function personForm(type,item=null){const product=type==='product',client=type==='client',employee=type==='employee';if(product){const p=item||{id:'',name:'',ean:'',code:'',category:'Alimentos',price:0,cost:0,wholesale:0,stock:0,minStock:0,unit:'Unidad',notes:''};openLayer(`${layerHead(p.id?'Editar producto':'Nuevo producto')}<div class="layer-tabs"><button class="tab active">Registro</button><button class="tab">Kit/Combo</button><button class="tab">Proveedores</button><button class="tab">Opciones</button></div><form id="productForm" data-id="${p.id}"><div class="layer-body"><div class="form-wrap"><div class="form-grid"><div class="field full"><label>Nombre</label><textarea name="name" placeholder="Campo obligatorio" required>${esc(p.name)}</textarea></div><div class="field"><label>EAN/GTIN</label><input name="ean" value="${esc(p.ean)}" placeholder="Use el lector o escriba"></div><div class="field"><label>Código</label><input name="code" value="${esc(p.code)}" required></div><div class="field"><label>Categoría</label><input name="category" value="${esc(p.category)}"></div><div class="field"><label>Unidad de medida</label><select name="unit">${['Unidad','Paquete','Caja','Bolsa','Botella','Lata','Kg'].map(x=>`<option ${p.unit===x?'selected':''}>${x}</option>`).join('')}</select></div><div class="field"><label>Precio de venta</label><input name="price" type="number" step="0.01" value="${p.price}"></div><div class="field"><label>Precio de costo</label><input name="cost" type="number" step="0.01" value="${p.cost}"></div><div class="field"><label>Precio mayorista</label><input name="wholesale" type="number" step="0.01" value="${p.wholesale}"></div><div class="field"><label>Stock actual</label><input name="stock" type="number" step="0.001" value="${p.stock}"></div><div class="field"><label>Stock mínimo</label><input name="minStock" type="number" step="0.001" value="${p.minStock}"></div><div class="field full"><label>Observaciones</label><textarea name="notes">${esc(p.notes)}</textarea></div></div></div></div><footer class="layer-foot"><button type="button" class="btn" data-action="close-layer">Cancelar</button><button class="btn primary" type="submit">Guardar</button></footer></form>`);return}
 const x=item||{};const title=client?(x.id?'Editar cliente':'Nuevo cliente'):(x.id?'Editar funcionario':'Nuevo funcionario');openLayer(`${layerHead(title)}${employee?'<div class="layer-tabs"><button class="tab active">Datos personales</button><button class="tab">Supervisor</button><button class="tab">Permisos</button></div>':''}<form id="${client?'clientForm':'employeeForm'}" data-id="${x.id||''}"><div class="layer-body"><div class="form-wrap"><div class="form-grid"><div class="field full"><label>Nombre completo ${client?'/ Empresa':''}</label><input name="name" value="${esc(x.name||'')}" placeholder="Campo obligatorio" required></div>${client?`<div class="field"><label>Tipo</label><select name="type"><option>Minorista</option><option ${x.type==='Mayorista'?'selected':''}>Mayorista</option></select></div><div class="field"><label>Código</label><input name="code" value="${esc(x.code||'')}"></div>`:`<div class="field"><label>Cargo</label><input name="role" value="${esc(x.role||'')}" required></div><div class="field"><label>Email</label><input name="email" type="email" value="${esc(x.email||'')}" required></div>`}<div class="field"><label>CI / NIT</label><input name="document" value="${esc(x.document||'')}"></div><div class="field"><label>Celular</label><input name="phone" value="${esc(x.phone||'')}" placeholder="Solo números"></div>${client?`<div class="field"><label>Ciudad</label><input name="city" value="${esc(x.city||'Cobija')}"></div><div class="field"><label>${x.id?'Saldo actual':'Saldo inicial'}</label><input name="balance" type="number" step="0.01" value="${x.balance||0}" ${x.id?'readonly':''}>${x.id?'<small>Use “Corregir saldo” en la cuenta para conservar el historial.</small>':''}</div><div class="field full"><label>Foto de perfil</label><input name="photo" type="file" accept="image/*"><small>JPG, PNG o WebP. La foto actual se conserva si no elige otra.</small></div><div class="field full"><label>Observaciones</label><textarea name="notes">${esc(x.notes||'')}</textarea></div>`:`<div class="field full"><label>Foto de perfil</label><input name="photo" type="file" accept="image/*"></div><div class="field full"><div class="switch-line"><strong>Este usuario es supervisor</strong><button type="button" class="switch ${x.supervisor?'on':''}" data-form-switch="supervisor"></button><input type="hidden" name="supervisor" value="${x.supervisor?'1':'0'}"></div><div class="switch-line"><strong>Este usuario es administrador</strong><button type="button" class="switch ${x.admin?'on':''}" data-form-switch="admin"></button><input type="hidden" name="admin" value="${x.admin?'1':'0'}"></div></div>`}</div></div></div><footer class="layer-foot"><button type="button" class="btn" data-action="${client&&x.id?'back-client':'close-layer'}" ${client&&x.id?`data-id="${x.id}"`:''}>Cancelar</button><button class="btn primary" type="submit">Guardar</button></footer></form>`)}

function ensureFormPanels(form){
 if(!form||form.dataset.panelsReady)return;form.dataset.panelsReady='1';const body=form.querySelector('.layer-body'),main=form.querySelector('.form-wrap');main.dataset.formPanel='0';main.classList.add('form-tab-panel');
 if(form.id==='productForm'){const p=state.products.find(x=>x.id===form.dataset.id)||{};body.insertAdjacentHTML('beforeend',`<section class="form-tab-panel" data-form-panel="1" hidden><div class="form-tab-intro"><span>${svg('box')}</span><div><h3>Kit / Combo</h3><p>Configure si este producto representa un conjunto de artículos.</p></div></div><div class="form-grid"><div class="field full"><div class="switch-line"><div><strong>Producto compuesto</strong><small>Identifica el artículo como kit o combo.</small></div><button type="button" class="switch ${p.isKit?'on':''}" data-form-switch="isKit"></button><input type="hidden" name="isKit" value="${p.isKit?'1':'0'}"></div></div><div class="field full"><label>Componentes del kit</label><textarea name="kitComponents" rows="6" placeholder="Ej.: 2 × Filtro de aceite&#10;1 × Lubricante 1 L">${esc(p.kitComponents||'')}</textarea><small>Use una línea para cada componente y cantidad.</small></div></div></section><section class="form-tab-panel" data-form-panel="2" hidden><div class="form-tab-intro"><span>${svg('store')}</span><div><h3>Proveedor</h3><p>Datos comerciais usados en compras e importaciones.</p></div></div><div class="form-grid"><div class="field"><label>Proveedor principal</label><input name="supplier" value="${esc(p.supplier||'')}"></div><div class="field"><label>Código del proveedor</label><input name="supplierCode" value="${esc(p.supplierCode||'')}"></div><div class="field"><label>Último costo</label><input name="lastCost" type="number" min="0" step="0.01" value="${Number(p.lastCost??p.cost??0)}"></div><div class="field"><label>Contacto / referencia</label><input name="supplierNote" value="${esc(p.supplierNote||'')}"></div></div></section><section class="form-tab-panel" data-form-panel="3" hidden><div class="form-tab-intro"><span>${svg('settings')}</span><div><h3>Opciones del producto</h3><p>Controle visibilidad y comportamiento en el PDV.</p></div></div><div class="option-stack"><div class="switch-line"><div><strong>Producto activo</strong><small>Aparece en búsquedas y en el catálogo del PDV.</small></div><button type="button" class="switch ${p.active!==false?'on':''}" data-form-switch="active"></button><input type="hidden" name="active" value="${p.active!==false?'1':'0'}"></div><div class="switch-line"><div><strong>Permitir precio editable</strong><small>El funcionario puede ajustar el precio durante la venta.</small></div><button type="button" class="switch ${p.editablePrice!==false?'on':''}" data-form-switch="editablePrice"></button><input type="hidden" name="editablePrice" value="${p.editablePrice!==false?'1':'0'}"></div></div></section>`)}
 if(form.id==='employeeForm'){const e=state.employees.find(x=>x.id===form.dataset.id)||{},permissions=e.permissions||{sales:true,clients:true,stock:false,cash:false,reports:false};const switches=main.querySelector('.field.full:has([data-form-switch="supervisor"])');body.insertAdjacentHTML('beforeend',`<section class="form-tab-panel" data-form-panel="1" hidden><div class="form-tab-intro"><span>${svg('badge')}</span><div><h3>Jerarquía y supervisión</h3><p>Defina el nivel de acceso administrativo del funcionario.</p></div></div><div class="supervisor-panel-target"></div></section><section class="form-tab-panel" data-form-panel="2" hidden><div class="form-tab-intro"><span>${svg('settings')}</span><div><h3>Permisos del sistema</h3><p>Elija los módulos que este funcionario podrá consultar.</p></div></div><div class="permission-grid">${[['sales','Ventas'],['clients','Clientes'],['stock','Inventario'],['cash','Caja'],['reports','Informes']].map(([key,label])=>`<label><input type="checkbox" name="permission_${key}" ${permissions[key]?'checked':''}><span>${svg(key==='sales'?'cart':key==='clients'?'users':key==='stock'?'box':key==='cash'?'wallet':'chart')}<strong>${label}</strong></span></label>`).join('')}</div></section>`);if(switches)body.querySelector('.supervisor-panel-target').append(switches)}
 injectIcons(body);applyLanguageDOM(body)
}
function activateFormTab(button){const form=button.closest('.layer-card')?.querySelector('form'),buttons=[...button.parentElement.querySelectorAll('.tab')],index=buttons.indexOf(button);if(!form||index<0)return;ensureFormPanels(form);buttons.forEach((tab,i)=>tab.classList.toggle('active',i===index));form.querySelectorAll('[data-form-panel]').forEach(panel=>panel.hidden=Number(panel.dataset.formPanel)!==index)}

function clientLedger(c){const ordered=(c.ledger||[]).slice().sort((a,b)=>new Date(a.date)-new Date(b.date)),delta=ordered.reduce((n,l)=>n+(l.type==='credit'?Number(l.amount):-Number(l.amount)),0);let running=Number(c.balance||0)-delta;return ordered.map(l=>{running+=l.type==='credit'?Number(l.amount):-Number(l.amount);return{...l,running}})}
function clientActivity(c){const movements=(c.ledger||[]).map((l,index)=>({id:`ledger-${index}`,date:l.date,kind:l.type,title:l.description,meta:l.type==='credit'?'Movimiento a favor':'Movimiento en cuenta',amount:Number(l.amount)})),sales=state.sales.filter(s=>s.client===c.name).map(s=>({id:s.id,date:s.date,kind:'sale',title:`Venta ${s.id}`,meta:`${s.items} artículos · ${s.payment} · ${s.employee}`,amount:Number(s.total)}));return[...movements,...sales].sort((a,b)=>new Date(b.date)-new Date(a.date))}
function clientTimeline(c){
 const activity=clientActivity(c);let lastDay='';
 const html=activity.map(item=>{const day=new Date(item.date).toISOString().slice(0,10),separator=day!==lastDay?`<div class="timeline-day"><span>${new Intl.DateTimeFormat(locale(),{weekday:'long',day:'numeric',month:'long',year:'numeric'}).format(new Date(item.date))}</span></div>`:'';lastDay=day;return`${separator}<article class="timeline-item ${item.kind}"><span class="timeline-dot">${svg(item.kind==='sale'?'receipt':item.kind==='credit'?'cash':'wallet')}</span><div class="timeline-card"><div><strong>${esc(item.title)}</strong><small>${time(item.date)} · ${esc(item.meta)}</small></div><b class="${item.kind==='credit'?'money-positive':item.kind==='debit'?'money-negative':''}">${item.kind==='credit'?'+ ':item.kind==='debit'?'− ':''}${money(item.amount)}</b></div></article>`}).join('');
 return `<div class="client-section-head"><div><span>Actividad</span><h2>Ventas e historial</h2></div><span class="history-count">${activity.length} eventos</span></div><div class="timeline">${html||`<div class="timeline-empty">${svg('calendar')}<strong>Sin actividad todavía</strong><span>Las ventas y movimientos aparecerán aquí.</span></div>`}</div>`;
}
function ledgerAction(l){const payment=/^pago/i.test(l.description||''),adjust=/correcci[oó]n|ajuste/i.test(l.description||''),credit=l.type==='credit';return{sign:credit?'+':'−',code:payment?'PAG':adjust?'AJU':credit?'CRÉ':'DÉB',tone:credit?'credit':'debit'}}
function clientProfilePanel(c){
 const tab=ui.clientProfileTab,entries=clientLedger(c),balanceClass=c.balance<0?'debt':c.balance>0?'credit':'settled',balanceLabel=c.balance<0?'Debe':c.balance>0?'Crédito disponible':'Cuenta al día';
 const tabs=`<nav class="client-profile-tabs" aria-label="Secciones del cliente">${[['account','Cuenta','wallet'],['sales','Ventas','receipt'],['data','Datos','users']].map(([key,label,icon])=>`<button class="${tab===key?'active':''}" data-client-profile-tab="${key}" data-client="${c.id}">${svg(icon)}<span>${label}</span></button>`).join('')}</nav>`;
 if(tab==='sales')return `${tabs}<section class="client-tab-panel client-sales-panel">${clientTimeline(c)}</section>`;
 if(tab==='data')return `${tabs}<section class="client-tab-panel"><div class="client-section-head"><div><span>Perfil</span><h2>Datos del cliente</h2></div><button class="btn" data-client-edit="${c.id}">${svg('edit')} Editar datos</button></div><div class="client-data-grid"><div><span>Código</span><strong>${esc(c.code||'—')}</strong></div><div><span>CI / NIT</span><strong>${esc(c.document||'No informado')}</strong></div><div><span>Celular</span><strong>${esc(c.phone||'No informado')}</strong></div><div><span>Ciudad</span><strong>${esc(c.city||'No informada')}</strong></div><div><span>Tipo</span><strong>${esc(c.type||'—')}</strong></div><div class="wide"><span>Observaciones</span><strong>${esc(c.notes||'Sin observaciones')}</strong></div></div><div class="client-data-metrics"><div><span>Compras</span><strong>${Number(c.purchases||0)}</strong></div><div><span>Total comprado</span><strong>${money(c.total)}</strong></div></div></section>`;
 const rows=entries.slice().reverse().map(l=>{const action=ledgerAction(l);return`<div class="client-ledger-row"><div class="client-ledger-action ${action.tone}" title="${action.code}"><b>${action.sign}</b><span>${action.code}</span></div><div class="client-ledger-description"><strong>${esc(l.description)}</strong></div><div class="client-ledger-date"><strong>${date(l.date)}</strong><small>${time(l.date)}</small></div><div class="client-ledger-value ${l.type==='credit'?'money-positive':'money-negative'}">${action.sign} ${money(l.amount)}</div><div class="client-ledger-balance">${l.running<0?'− ':l.running>0?'+ ':''}${money(Math.abs(l.running))}</div></div>`}).join('');
 const blanks=Array.from({length:Math.max(0,6-entries.length)},()=>`<div class="client-ledger-row client-ledger-empty-row"><div></div><div><span>—</span></div><div></div><div></div><div></div></div>`).join('');
 return `${tabs}<section class="client-tab-panel"><div class="client-account-hero"><div><span>Cuenta corriente</span><strong class="${balanceClass}">${c.balance<0?'− ':c.balance>0?'+ ':''}${money(Math.abs(c.balance))}</strong><small>${balanceLabel}</small></div><span class="account-orbit">${svg('wallet')}</span></div><div class="client-account-actions"><button data-ledger="debit" data-client="${c.id}"><span>−</span><div><strong>Débito</strong><small>Agregar deuda</small></div></button><button data-ledger="credit" data-client="${c.id}"><span>+</span><div><strong>Crédito</strong><small>Saldo a favor</small></div></button><button data-ledger="payment" data-client="${c.id}"><span>${svg('cash')}</span><div><strong>Pago</strong><small>Cobro parcial o total</small></div></button><button data-ledger="adjust" data-client="${c.id}"><span>${svg('edit')}</span><div><strong>Ajuste</strong><small>Ajustar saldo</small></div></button></div><div class="client-ledger-sheet"><div class="client-ledger-row client-ledger-head"><span>Acción</span><span>Concepto</span><span>Fecha</span><span>Valor</span><span>Saldo</span></div>${rows}${blanks}<footer class="client-sheet-total"><span>Situación final</span><strong class="${balanceClass}">${c.balance<0?'− ':c.balance>0?'+ ':''}${money(Math.abs(c.balance))}</strong></footer></div></section>`;
}
function refreshClientProfileTab(id,tab){const c=state.clients.find(x=>x.id===id),main=$('#clientProfileMain');if(!c||!main)return;ui.clientProfileTab=tab;main.innerHTML=clientProfilePanel(c);injectIcons(main);applyLanguageDOM(main)}
function clientAccount(id,resetTab=true){
 const c=state.clients.find(x=>x.id===id);if(!c)return;if(resetTab||ui.activeClientId!==id)ui.clientProfileTab='account';ui.activeClientId=id;
 openLayer(`<section class="client-workspace"><header class="client-workspace-head"><button class="client-back" data-action="close-layer">${svg('close')}<span>Clientes</span></button><div><small>Ficha del cliente</small><h1>${esc(c.name)}</h1></div><button class="btn" data-client-edit="${c.id}">${svg('edit')} Editar datos</button></header><div class="client-workspace-grid client-sheet-layout"><aside class="client-identity"><div class="client-photo">${avatar(c.photo,c.name,'profile-avatar')}</div><span class="client-kind">${esc(c.type)}</span><h2>${esc(c.name)}</h2><p>${esc(c.notes||'Sin observaciones')}</p><dl><div><dt>Código</dt><dd>${esc(c.code||'—')}</dd></div><div><dt>CI / NIT</dt><dd>${esc(c.document||'No informado')}</dd></div><div><dt>Celular</dt><dd>${esc(c.phone||'No informado')}</dd></div><div><dt>Ciudad</dt><dd>${esc(c.city||'No informada')}</dd></div></dl><div class="client-mini-metrics"><div><strong>${Number(c.purchases||0)}</strong><span>Compras</span></div><div><strong>${money(c.total)}</strong><span>Total comprado</span></div></div></aside><main class="client-profile-main" id="clientProfileMain">${clientProfilePanel(c)}</main></div></section>`,'client-full');
}
function ledgerForm(id,type){const c=state.clients.find(x=>x.id===id);if(!c)return;const config={debit:['Añadir débito','Valor de la deuda','Compra fiada, ajuste o cargo'],credit:['Añadir crédito','Valor a favor','Crédito, devolución o anticipo'],payment:['Registrar pago','Valor recibido','Pago de deuda'],adjust:['Corregir saldo','Nuevo saldo final','Corrección manual de saldo']}[type]||['Movimiento','Valor','Movimiento'];openLayer(`${layerHead(config[0])}<form id="ledgerForm" data-id="${id}" data-type="${type}"><div class="layer-body"><div class="movement-client">${avatar(c.photo,c.name)}<div><strong>${esc(c.name)}</strong><small>Saldo actual: ${c.balance<0?'− ':c.balance>0?'+ ':''}${money(Math.abs(c.balance))}</small></div></div><div class="form-grid"><div class="field"><label>${config[1]}</label><input name="amount" type="number" ${type==='adjust'?'':'min="0.01"'} step="0.01" ${type==='adjust'?`value="${c.balance}"`:''} required></div><div class="field"><label>Fecha</label><input name="date" type="date" value="${new Date().toISOString().slice(0,10)}" required></div>${type==='payment'?`<div class="field"><label>Forma de pago</label><select name="paymentMethod"><option>Efectivo</option><option>PIX</option><option>QR</option><option>Transferencia</option></select></div>`:''}<div class="field ${type==='payment'?'':'full'}"><label>Descripción</label><input name="description" value="${esc(config[2])}" required></div></div>${type==='adjust'?'<p class="form-warning">Se creará un movimiento de ajuste; ningún historial anterior será borrado.</p>':''}</div><footer class="layer-foot"><button type="button" class="btn" data-action="back-client" data-id="${id}">Cancelar</button><button class="btn primary">Guardar movimiento</button></footer></form>`,'modal narrow')}
function employeeProfile(id){const e=state.employees.find(x=>x.id===id);openLayer(`${layerHead('Perfil del funcionario')}<div class="layer-body"><div class="profile-head">${avatar(e.photo,e.name)}<div><h3>${esc(e.name)}</h3><p>${esc(e.role)} · ${esc(e.document)}</p></div><div class="profile-actions"><button class="btn" data-employee-edit="${e.id}">Editar</button></div></div><div class="metrics"><div class="metric"><span>Ventas</span><strong>${e.sales}</strong></div><div class="metric"><span>Facturado</span><strong>${money(e.total)}</strong></div><div class="metric"><span>Ticket promedio</span><strong>${money(e.ticket)}</strong></div><div class="metric"><span>Meta</span><strong>${e.goal}%</strong></div></div><div class="data-region"><table class="data-table"><thead><tr><th>Número</th><th>Fecha</th><th>Cliente</th><th>Pago</th><th>Total</th></tr></thead><tbody>${state.sales.filter(s=>s.employee===e.name).map(s=>`<tr><td>${s.id}</td><td>${date(s.date)}</td><td>${esc(s.client)}</td><td>${esc(s.payment)}</td><td>${money(s.total)}</td></tr>`).join('')}</tbody></table></div></div><footer class="layer-foot"><button class="btn" data-action="close-layer">Cerrar</button></footer>`,'modal')}
function cashForm(){openLayer(`${layerHead('Nueva transacción')}<form id="cashForm"><div class="layer-body"><div class="form-grid"><div class="field"><label>Tipo</label><select name="type"><option value="in">Entrada</option><option value="out">Salida</option></select></div><div class="field"><label>Valor</label><input name="amount" type="number" min="0.01" step="0.01" required></div><div class="field full"><label>Descripción</label><input name="description" required></div></div></div><footer class="layer-foot"><button type="button" class="btn" data-action="close-layer">Cancelar</button><button class="btn primary">Guardar</button></footer></form>`,'modal narrow')}
function quickSettings(){const o=state.settings.options;openLayer(`${layerHead('Configuración de productos')}<div class="settings-layout"><nav class="settings-nav"><button data-form-info="Ventas">Ventas</button><button class="active">Productos</button></nav><div class="settings-panel"><h2>Productos</h2>${[['quickProduct','Usar registro rápido de productos'],['duplicateCode','Permitir productos con el mismo código'],['blockNoStock','No permitir venta sin stock'],['autoPrice','Calcular precio automáticamente']].map(([key,label])=>`<div class="switch-line"><strong>${label}</strong><button class="switch ${o[key]?'on':''}" data-setting-toggle="${key}" aria-label="Alternar ${esc(label)}"></button></div>`).join('')}</div></div><footer class="layer-foot"><button class="btn primary" data-action="close-layer">Listo</button></footer>`,'modal')}
function cycleUi(key,values){const index=Math.max(0,values.indexOf(ui[key]));ui[key]=values[(index+1)%values.length];render()}
function saleView(id){const s=state.sales.find(x=>x.id===id);if(!s)return;openLayer(`${layerHead(`Venta ${s.id}`)}<div class="layer-body"><div class="detail-hero"><span class="detail-icon">${svg('receipt')}</span><div><small>${esc(s.type)}</small><h2>${money(s.total)}</h2></div><span class="status blue">${esc(s.payment)}</span></div><div class="detail-grid"><div><span>Cliente</span><strong>${esc(s.client)}</strong></div><div><span>Vendedor</span><strong>${esc(s.employee)}</strong></div><div><span>Fecha</span><strong>${date(s.date)} · ${time(s.date)}</strong></div><div><span>Artículos</span><strong>${s.items}</strong></div></div></div><footer class="layer-foot">${s.receiptItems?.length?`<button class="btn" data-receipt-view="${esc(s.id)}">${svg('receipt')} Ver cupón</button>`:''}<button class="btn primary" data-action="close-layer">Listo</button></footer>`,'modal narrow')}
function cashMovementView(id){const m=state.cash.find(x=>x.id===id);if(!m)return;openLayer(`${layerHead('Detalle de caja')}<div class="layer-body"><div class="detail-hero"><span class="detail-icon ${m.type==='out'?'out':''}">${svg(m.type==='in'?'cash':'wallet')}</span><div><small>${m.type==='in'?'Entrada':'Salida'}</small><h2 class="${m.type==='in'?'money-positive':'money-negative'}">${m.type==='in'?'+ ':'− '}${money(m.amount)}</h2></div></div><div class="detail-grid"><div><span>Descripción</span><strong>${esc(m.description)}</strong></div><div><span>Funcionario</span><strong>${esc(m.employee)}</strong></div><div><span>Fecha y hora</span><strong>${date(m.date)} · ${time(m.date)}</strong></div><div><span>Identificador</span><strong>${esc(m.id)}</strong></div></div></div><footer class="layer-foot"><button class="btn primary" data-action="close-layer">Listo</button></footer>`,'modal narrow')}
function stockForm(id){const p=state.products.find(x=>x.id===id);if(!p)return;openLayer(`${layerHead('Movimiento de inventario')}<form id="stockForm" data-id="${p.id}"><div class="layer-body"><div class="movement-client"><span class="item-thumb">${esc(p.name[0])}</span><div><strong>${esc(p.name)}</strong><small>Stock actual: ${p.stock.toFixed(3)} ${esc(p.unit)}</small></div></div><div class="form-grid"><div class="field"><label>Tipo</label><select name="type"><option value="in">Entrada</option><option value="out">Salida</option><option value="set">Corrección de saldo</option></select></div><div class="field"><label>Cantidad</label><input name="amount" type="number" min="0" step="0.001" required></div><div class="field full"><label>Motivo</label><input name="reason" required placeholder="Compra, avería, inventario físico..."></div></div></div><footer class="layer-foot"><button type="button" class="btn" data-action="close-layer">Cancelar</button><button class="btn primary">Guardar movimiento</button></footer></form>`,'modal narrow')}
function inventorySummary(){const cost=sum(state.products.map(p=>({amount:p.stock*p.cost}))),sale=sum(state.products.map(p=>({amount:p.stock*p.price}))),units=sum(state.products,'stock');openLayer(`${layerHead('Valor del inventario')}<div class="layer-body"><div class="detail-hero"><span class="detail-icon">${svg('box')}</span><div><small>Valor potencial de venta</small><h2>${money(sale)}</h2></div></div><div class="detail-grid"><div><span>Valor de costo</span><strong>${money(cost)}</strong></div><div><span>Margen potencial</span><strong>${money(sale-cost)}</strong></div><div><span>Unidades registradas</span><strong>${units.toLocaleString('es-BO')}</strong></div><div><span>Productos con alerta</span><strong>${state.products.filter(p=>p.stock<=p.minStock).length}</strong></div></div></div><footer class="layer-foot"><button class="btn primary" data-action="close-layer">Listo</button></footer>`,'modal narrow')}
function manageCategories(){const categories=[...new Set(state.products.map(p=>p.category))];openLayer(`${layerHead('Categorías de productos')}<div class="layer-body"><div class="category-manage-list">${categories.map(c=>`<div><span>${esc(c)}</span><strong>${state.products.filter(p=>p.category===c).length} productos</strong></div>`).join('')}</div><p class="form-warning">Para crear una categoría nueva, escríbala al registrar o editar un producto. Se incorporará automáticamente.</p></div><footer class="layer-foot"><button class="btn primary" data-action="close-layer">Listo</button></footer>`,'modal narrow')}
function catalogConfig(){openLayer(`${layerHead('Configurar catálogo')}<div class="layer-body"><div class="detail-hero"><span class="detail-icon">${svg('store')}</span><div><small>Catálogo white label</small><h2>${esc(state.settings.businessName)}</h2></div><span class="status blue">Preparado</span></div><div class="detail-grid"><div><span>Productos disponibles</span><strong>${state.products.filter(p=>p.stock>0).length}</strong></div><div><span>Moneda</span><strong>Boliviano (Bs)</strong></div><div><span>Precio mayorista</span><strong>${state.settings.wholesale?'Visible':'Oculto'}</strong></div><div><span>Canal</span><strong>Web adaptable</strong></div></div><p class="form-warning">La publicación externa del catálogo quedará vinculada al dominio de cada empresa al conectar el backend.</p></div><footer class="layer-foot"><button class="btn" data-action="close-layer">Cerrar</button><button class="btn primary" data-action="catalog-ready">Guardar configuración</button></footer>`,'modal narrow')}

function addCart(id){const p=state.products.find(x=>x.id===id);if(!p)return;if(state.settings.options.blockNoStock&&p.stock<=0)return toast('Producto sin stock','error');const requested=Math.max(1,Number(ui.posQty)||1),qty=state.settings.options.blockNoStock?Math.min(p.stock,requested):requested,existing=ui.cart.find(x=>x.id===id);if(existing)existing.qty=state.settings.options.blockNoStock?Math.min(p.stock,existing.qty+qty):existing.qty+qty;else{const base=ui.wholesale?Number(p.wholesale||p.price):Number(p.price);ui.cart.push({id:p.id,name:p.name,code:p.code,qty,basePrice:base,price:base,discount:0})}ui.posSearch='';ui.posQty=1;if($('#posSearch'))$('#posSearch').value='';if($('#posQty'))$('#posQty').value='1';refreshPosView();$('#posSearch')?.focus()}
const newClientSaleId=()=>globalThis.crypto?.randomUUID?.()||`sale-${Date.now()}-${Math.random().toString(16).slice(2)}`;
function receiptLayer(sale){ui.lastReceipt=sale;openLayer(`${layerHead('Venta registrada',false)}${saleSteps(3)}<div class="layer-body"><article class="receipt-paper" id="printReceipt"><header><strong>${esc(state.settings.businessName)}</strong><span>${esc(state.settings.legalName||'')}</span><span>NIT: ${esc(state.settings.nit||'—')}</span><span>${esc(state.settings.city||'')}</span></header><div class="receipt-number"><span>CUPÓN / COMPROBANTE</span><b>${esc(sale.id)}</b><small>${date(sale.date)} · ${time(sale.date)}</small></div><div class="receipt-lines">${(sale.receiptItems||[]).map(item=>`<div><span>${Number(item.qty)} × ${esc(item.name)}${item.discount?` <small>(−${item.discount}%)</small>`:''}</span><b>${money(posItemTotal(item))}</b></div>`).join('')}</div><div class="receipt-summary"><span>Subtotal <b>${money(sale.subtotal)}</b></span><span>Descuento <b>− ${money(sale.discountTotal)}</b></span><strong>Total <b>${money(sale.total)}</b></strong><span>Pago <b>${esc(sale.payment)}</b></span><span>Recibido <b>${money(sale.received)}</b></span><span>Cambio <b>${money(sale.change)}</b></span></div><footer><span>Cliente: ${esc(sale.client)}</span><span>Atendido por: ${esc(sale.employee)}</span><span>${esc(sale.delivery||'Retiro en tienda')}</span>${sale.notes?`<span>Obs.: ${esc(sale.notes)}</span>`:''}<b class="receipt-sync ${sale.syncStatus}">${sale.syncStatus==='pending'?'Pendiente de sincronización':sale.syncStatus==='local'?'Guardado en este dispositivo':'Sincronizado'}</b><small>Este comprobante no sustituye una factura fiscal integrada al SIN.</small></footer></article></div><footer class="layer-foot receipt-actions"><button class="btn" data-action="print-current-receipt">${svg('receipt')} Imprimir cupón</button><button class="btn primary" data-action="new-sale">Nueva venta</button></footer>`,'modal narrow receipt-modal')}
async function completeSale(payment,received){
 const total=posTotal(),subtotal=posSubtotal(),client=state.clients.find(c=>c.id===ui.posClient),employee=currentEmployee();if(!ui.cart.length)return;ensureCashSession();
 const receivedValue=Number(received||total);if(payment==='Efectivo'&&receivedValue<total)throw Error('El valor recibido es menor que el total');
 const clientSaleId=newClientSaleId(),id=`V${String(1000+state.sales.length+1).padStart(4,'0')}`,createdAt=new Date().toISOString(),payload={clientSaleId,clientId:client?.id||null,payment,notes:[ui.posNotes,ui.posDelivery].filter(Boolean).join(' · '),items:ui.cart.map(item=>({product_id:item.id,quantity:item.qty,unit_price:item.price,discount:Number(item.discount||0)}))};
 let syncStatus=GrassiBackend.isRemote()?'pending':'local';if(GrassiBackend.isRemote()&&navigator.onLine){try{await GrassiBackend.registerSale(payload);syncStatus='synced'}catch{syncStatus='pending'}}
 const recordType=ui.saleMode||'Venta',cost=ui.cart.reduce((n,item)=>n+Number(state.products.find(p=>p.id===item.id)?.cost||0)*Number(item.qty||0),0),sale={id,clientSaleId,date:createdAt,client:client?.name||'Consumidor final',employee:employee?.name||authSession.name,employeeId:authSession.userId,items:ui.cart.reduce((n,i)=>n+i.qty,0),total,cost,subtotal,discountTotal:Math.max(0,subtotal-total),received:receivedValue,change:Math.max(0,receivedValue-total),payment,type:recordType,notes:ui.posNotes,delivery:ui.posDelivery,receiptItems:structuredClone(ui.cart),syncStatus};
 if(!state.sales.some(x=>x.clientSaleId===clientSaleId))state.sales.unshift(sale);if(syncStatus==='pending'&&!state.offlineQueue.some(x=>x.clientSaleId===clientSaleId))state.offlineQueue.push(payload);
 if(recordType==='Venta'){ui.cart.forEach(i=>{const p=state.products.find(x=>x.id===i.id);if(p)p.stock=Math.max(0,p.stock-i.qty)});if(employee){employee.sales++;employee.total+=total;employee.ticket=employee.total/employee.sales}if(client){client.purchases++;client.total+=total;if(payment==='Cuenta cliente'){client.balance-=total;client.ledger.push({date:createdAt,type:'debit',description:`Venta #${id}`,amount:total})}}if(payment!=='Cuenta cliente')state.cash.unshift({id:`M${Date.now()}`,date:createdAt,type:'in',description:`Venta #${id} — ${payment}`,amount:total,employee:employee?.name||authSession.name})}save();receiptLayer(sale);toast(syncStatus==='pending'?`${recordType} ${id} guardado sin conexión`:`${recordType} ${id} registrado`);if(syncStatus==='pending')syncOfflineSales()
}
async function syncOfflineSales(){if(!navigator.onLine||!GrassiBackend.isRemote()||!state.offlineQueue.length)return;for(const payload of [...state.offlineQueue]){try{await GrassiBackend.registerSale(payload);state.offlineQueue=state.offlineQueue.filter(x=>x.clientSaleId!==payload.clientSaleId);const sale=state.sales.find(x=>x.clientSaleId===payload.clientSaleId);if(sale)sale.syncStatus='synced';save()}catch{break}}const status=queueStatus(),pill=$('#syncPill');if(pill){pill.className=`sync-pill ${status[1]}`;pill.textContent=`● ${status[0]}`}}

function toast(message,type=''){const e=document.createElement('div');e.className=`toast ${type}`;e.innerHTML=`${svg(type?'close':'check')}<span>${esc(message)}</span>`;$('#toastRegion').append(e);setTimeout(()=>e.remove(),3200)}
function toCSV(rows){if(!rows.length)return'';const h=Object.keys(rows[0]),q=v=>`"${String(v??'').replace(/"/g,'""')}"`;return[h.map(q).join(','),...rows.map(r=>h.map(k=>q(r[k])).join(','))].join('\n')}
function parseCSV(t){const rows=[];let row=[],v='',q=false;for(let i=0;i<t.length;i++){const c=t[i],n=t[i+1];if(c==='"'&&q&&n==='"'){v+='"';i++}else if(c==='"')q=!q;else if(c===','&&!q){row.push(v);v=''}else if((c==='\n'||c==='\r')&&!q){if(c==='\r'&&n==='\n')i++;row.push(v);if(row.some(x=>x.trim()))rows.push(row);row=[];v=''}else v+=c}if(v||row.length){row.push(v);rows.push(row)}const h=(rows.shift()||[]).map(x=>x.replace(/^\ufeff/,'').trim());return rows.map(r=>Object.fromEntries(h.map((x,i)=>[x,r[i]??''])))}
function download(name,content,type='text/plain'){const b=new Blob([content],{type}),u=URL.createObjectURL(b),a=document.createElement('a');a.href=u;a.download=name;a.click();URL.revokeObjectURL(u)}
function exportCSV(type){const rows=type==='products'?state.products:type==='clients'?state.clients.map(({photo,ledger,...c})=>({...c,ledger:JSON.stringify(ledger)})):state.employees.map(({photo,...e})=>e);download(`grassi-${type}.csv`,'\ufeff'+toCSV(rows),'text/csv;charset=utf-8');toast('Archivo CSV generado')}
async function fileData(file){if(!file?.size)return'';return await new Promise((res,rej)=>{const r=new FileReader();r.onload=()=>res(r.result);r.onerror=rej;r.readAsDataURL(file)})}

document.addEventListener('click',e=>{
 const feedback=e.target.closest('button,.btn,.plan-button');if(feedback&&!feedback.disabled){feedback.classList.remove('tap-feedback');void feedback.offsetWidth;feedback.classList.add('tap-feedback');setTimeout(()=>feedback.classList.remove('tap-feedback'),360)}
 const demo=e.target.closest('[data-demo-login]');if(demo){const admin=demo.dataset.demoLogin==='admin',form=$('#loginForm');form.elements.email.value=admin?'admin@example.invalid':'employee@example.invalid';form.elements.password.value=admin?'admin123':'func123';return form.requestSubmit()}
 const view=e.target.closest('[data-view]');if(view)return setView(view.dataset.view);
 const salesTab=e.target.closest('[data-sales-tab]');if(salesTab){ui.salesTab=salesTab.dataset.salesTab;return render()}
 const productTab=e.target.closest('[data-product-tab]');if(productTab){ui.productTab=productTab.dataset.productTab;return render()}
 const stockTab=e.target.closest('[data-stock-tab]');if(stockTab){ui.stockTab=stockTab.dataset.stockTab;return render()}
 const cat=e.target.closest('[data-category]');if(cat){ui.productCategory=cat.dataset.category;return render()}
 const report=e.target.closest('[data-report]');if(report){ui.report=report.dataset.report;return render()}
 const st=e.target.closest('[data-settings-tab]');if(st){ui.settingsTab=st.dataset.settingsTab;return render()}
	 const sale=e.target.closest('[data-sale-view]');if(sale)return saleView(sale.dataset.saleView);
	 const receipt=e.target.closest('[data-receipt-view]');if(receipt){const record=state.sales.find(x=>x.id===receipt.dataset.receiptView);if(record)return receiptLayer(record)}
	 const cv=e.target.closest('[data-client-view]');if(cv)return clientAccount(cv.dataset.clientView);
	 const clientProfileTab=e.target.closest('[data-client-profile-tab]');if(clientProfileTab)return refreshClientProfileTab(clientProfileTab.dataset.client,clientProfileTab.dataset.clientProfileTab);
	 const ce=e.target.closest('[data-client-edit]');if(ce)return personForm('client',state.clients.find(x=>x.id===ce.dataset.clientEdit));
 const pe=e.target.closest('[data-product-edit]');if(pe)return personForm('product',state.products.find(p=>p.id===pe.dataset.productEdit));
 const productRow=e.target.closest('.page-products tbody tr');if(productRow){const edit=productRow.querySelector('[data-product-edit]');if(edit)return personForm('product',state.products.find(p=>p.id===edit.dataset.productEdit))}
 const sm=e.target.closest('[data-stock-move]');if(sm)return stockForm(sm.dataset.stockMove);
 const cm=e.target.closest('[data-cash-view]');if(cm)return cashMovementView(cm.dataset.cashView);
 const ev=e.target.closest('[data-employee-view]');if(ev)return employeeProfile(ev.dataset.employeeView);
 const ee=e.target.closest('[data-employee-edit]');if(ee)return personForm('employee',state.employees.find(x=>x.id===ee.dataset.employeeEdit));
	 const add=e.target.closest('[data-pos-add]');if(add)return addCart(add.dataset.posAdd);
	 const mobilePosView=e.target.closest('[data-pos-mobile-view]');if(mobilePosView){ui.posMobileView=mobilePosView.dataset.posMobileView;const pdv=$('.pdv');pdv?.classList.toggle('mobile-view-products',ui.posMobileView==='products');pdv?.classList.toggle('mobile-view-cart',ui.posMobileView==='cart');$$('[data-pos-mobile-view]').forEach(x=>x.classList.toggle('active',x.dataset.posMobileView===ui.posMobileView));return}
	 const posCategory=e.target.closest('[data-pos-category]');if(posCategory){ui.posCategory=posCategory.dataset.posCategory;return refreshPosSearch()}
	 const rem=e.target.closest('[data-pos-remove]');if(rem){ui.cart=ui.cart.filter(i=>i.id!==rem.dataset.posRemove);return refreshPosView()}
	 const editCart=e.target.closest('[data-cart-edit]');if(editCart)return cartItemForm(editCart.dataset.cartEdit);
	 const inc=e.target.closest('[data-cart-inc]');if(inc){const item=ui.cart.find(i=>i.id===inc.dataset.cartInc),p=item&&state.products.find(x=>x.id===item.id);if(item)item.qty=state.settings.options.blockNoStock?Math.min(Number(p?.stock||item.qty),item.qty+1):item.qty+1;return refreshPosView()}
	 const dec=e.target.closest('[data-cart-dec]');if(dec){const item=ui.cart.find(i=>i.id===dec.dataset.cartDec);if(item)item.qty=Math.max(1,item.qty-1);return refreshPosView()}
	 const pay=e.target.closest('[data-pay]');if(pay&&!pay.disabled){ui.payment=pay.dataset.pay;$$('[data-pay]').forEach(x=>x.classList.toggle('active',x.dataset.pay===ui.payment));return}
	 const command=e.target.closest('[data-pos-command]');if(command)return runPosCommand(command.dataset.posCommand);
 const led=e.target.closest('[data-ledger]');if(led)return ledgerForm(led.dataset.client,led.dataset.ledger);
 const setting=e.target.closest('[data-setting-toggle]');if(setting){const key=setting.dataset.settingToggle;state.settings.options[key]=!state.settings.options[key];save();GrassiBackend.saveSettings(state.settings).catch(()=>{});setting.classList.toggle('on',state.settings.options[key]);return toast('Preferencia actualizada')}
 const formInfo=e.target.closest('[data-form-info]');if(formInfo)return toast(`${formInfo.dataset.formInfo}: disponible en la configuración completa`);
 const formTab=e.target.closest('.layer-tabs .tab');if(formTab)return activateFormTab(formTab);
 const sw=e.target.closest('[data-form-switch]');if(sw){sw.classList.toggle('on');const input=sw.parentElement.querySelector(`input[name="${sw.dataset.formSwitch}"]`);input.value=sw.classList.contains('on')?'1':'0';return}
 const act=e.target.closest('[data-action]')?.dataset.action;if(!act)return;
 const actions={
	  'toggle-mobile-menu':()=>{ui.menu=!$('#sidebar')?.classList.contains('open');$('#sidebar')?.classList.toggle('open',ui.menu)},
	  'open-pos':()=>openPos(true,'Venta'),'new-order':()=>openPos(true,'Pedido'),'new-budget':()=>openPos(true,'Presupuesto'),'close-layer':closeLayer,'open-help':helpCenter,'open-support':supportForm,'start-tutorial':startTutorial,'skip-tutorial':finishTutorial,'finish-tutorial':finishTutorial,'tutorial-next':()=>{ui.tutorialStep++;renderTutorial()},'tutorial-back':()=>{ui.tutorialStep=Math.max(0,ui.tutorialStep-1);renderTutorial()},'product-new':()=>personForm('product'),'client-new':()=>personForm('client'),'employee-new':()=>personForm('employee'),'payment':paymentLayer,'review-sale':reviewSaleLayer,'back-pos':renderPosLayer,'open-quick-settings':quickSettings,'cash-new':cashForm,'print-report':()=>window.print(),'print-labels':()=>window.print(),'user-menu':userMenu,
  'cycle-sales-payment':()=>cycleUi('salesPayment',['Todos','Efectivo','PIX','QR','Transferencia','Cuenta cliente']),'cycle-sales-period':()=>cycleUi('salesPeriod',['7 días','30 días','Todo el historial']),
  'cycle-client-sort':()=>cycleUi('clientSort',['Nombre','Saldo','Compras']),'cycle-client-filter':()=>cycleUi('clientFilter',['Todos','Con deuda','Con crédito']),
  'cycle-product-sort':()=>cycleUi('productSort',['Nombre','Stock','Precio']),'cycle-product-filter':()=>cycleUi('productFilter',['Todos','Stock bajo','Sin stock']),
  'cycle-stock-filter':()=>cycleUi('stockFilter',['Todos','Stock bajo','Sin stock']),'inventory-summary':inventorySummary,'manage-categories':manageCategories,'catalog-config':catalogConfig,'catalog-ready':()=>{closeLayer();toast('Configuración del catálogo guardada')},
  'cycle-cash-filter':()=>cycleUi('cashFilter',['Todos','Entradas','Salidas']),'cycle-employee-filter':()=>cycleUi('employeeFilter',['Todos','Supervisores','Meta baja']),'cycle-report-period':()=>cycleUi('reportPeriod',['7 días','30 días','Este mes']),'cycle-report-group':()=>cycleUi('reportGroup',['Día','Semana','Mes']),
	  'logout':()=>{GrassiBackend.logout();authSession=null;sessionStorage.removeItem('grassi.pos.unlocked');closeLayer();renderLogin()},
  'cycle-theme':()=>{const current=state.settings.appearance.mode;state.settings.appearance.mode=current==='light'?'dark':'light';save();closeLayer();if(appMode==='standalone-pos')renderPosLayer();else render()},
	  'preview-pos':openPos,'pos-delivery':posDeliveryForm,'pos-notes':posNotesForm,'pos-shortcuts':shortcutPanel,'print-current-receipt':()=>window.print(),'new-sale':()=>{closeLayer();openPos(true)},
	  'toggle-pos-panel':()=>{ui.posSidebarCollapsed=!ui.posSidebarCollapsed;renderPosLayer()},
	  'toggle-wholesale':toggleWholesalePos,
	  'pos-client':posClientPicker,
  'back-client':()=>clientAccount(e.target.closest('[data-id]').dataset.id),'toggle-wholesale-setting':()=>{state.settings.wholesale=!state.settings.wholesale;save();render()},
	  'cash-close':()=>closeCash('manual'),'cash-open':()=>{ensureCashSession();render();toast('Caja abierta correctamente')},
  'export-all':()=>{download(`grassi-respaldo-${new Date().toISOString().slice(0,10)}.json`,JSON.stringify(state,null,2),'application/json');toast('Respaldo generado')},
  'export-products':()=>exportCSV('products'),'export-clients':()=>exportCSV('clients'),'export-employees':()=>exportCSV('employees'),
  'import-backup':()=>{ui.importType='backup';$('#importInput').click()},'import-products':()=>{ui.importType='products';$('#importInput').click()},'import-clients':()=>{ui.importType='clients';$('#importInput').click()},'import-employees':()=>{ui.importType='employees';$('#importInput').click()}
 };actions[act]?.()
});
document.addEventListener('click',e=>{const s=e.target.closest('[data-select-client]');if(s){ui.posClient=s.dataset.selectClient;renderPosLayer()}});
document.addEventListener('input',e=>{if(e.target.id==='pageSearch'){ui.search=e.target.value;render();setTimeout(()=>{const i=$('#pageSearch');i?.focus();i?.setSelectionRange(ui.search.length,ui.search.length)},0)}if(e.target.id==='posSearch'){ui.posSearch=e.target.value;refreshPosSearch()}if(e.target.id==='posQty')ui.posQty=Number(e.target.value)||1});
document.addEventListener('change',e=>{if(e.target.matches('[data-language]')){state.settings.language=e.target.value==='pt'?'pt':'es';save();GrassiBackend.saveSettings(state.settings).catch(()=>{});location.reload()}if(e.target.matches('[data-cart-qty]')){const item=ui.cart.find(i=>i.id===e.target.dataset.cartQty),p=item&&state.products.find(x=>x.id===item.id),requested=Math.max(1,Number(e.target.value)||1);if(item)item.qty=state.settings.options.blockNoStock?Math.min(Number(p?.stock||requested),requested):requested;refreshPosView()}});
document.addEventListener('submit',async e=>{
 e.preventDefault();const f=e.target,d=Object.fromEntries(new FormData(f).entries());
	 if(f.id==='loginForm'){const submit=f.querySelector('[type="submit"]');submit.disabled=true;submit.textContent='Ingresando…';try{const session=await GrassiBackend.login(d.email,d.password);if(appMode==='standalone-pos')sessionStorage.setItem('grassi.pos.unlocked','1');return await startAuthenticated(session)}catch(error){return renderLogin(error.message)} }
 try{
  if(f.id==='supportForm'){const link=document.createElement('a');link.href=supportWhatsAppUrl(d);link.target='_blank';link.rel='noopener noreferrer';document.body.appendChild(link);link.click();link.remove();toast('Abriendo WhatsApp con su mensaje preparado');return}
  if(f.id==='productForm'){const old=state.products.find(x=>x.id===f.dataset.id);let p={...old,id:f.dataset.id||`P${Date.now()}`,name:d.name,ean:d.ean,code:d.code,category:d.category,price:Number(d.price),cost:Number(d.cost),wholesale:Number(d.wholesale),stock:Number(d.stock),minStock:Number(d.minStock),unit:d.unit,notes:d.notes,isKit:d.isKit==null?Boolean(old?.isKit):d.isKit==='1',kitComponents:d.kitComponents??old?.kitComponents??'',supplier:d.supplier??old?.supplier??'',supplierCode:d.supplierCode??old?.supplierCode??'',lastCost:Number(d.lastCost??old?.lastCost??d.cost??0),supplierNote:d.supplierNote??old?.supplierNote??'',active:d.active==null?old?.active!==false:d.active==='1',editablePrice:d.editablePrice==null?old?.editablePrice!==false:d.editablePrice==='1'};const oldId=p.id,i=state.products.findIndex(x=>x.id===oldId),saved=await GrassiBackend.saveProduct(p);if(saved?.id)p.id=saved.id;i>=0?state.products[i]=p:state.products.unshift(p);save();closeLayer();render();toast(i>=0?'Producto actualizado':'Producto creado')}
	  if(f.id==='clientForm'){const old=state.clients.find(x=>x.id===f.dataset.id),photo=await fileData(f.elements.photo.files[0]);let x={id:f.dataset.id||`C${Date.now()}`,name:d.name,type:d.type,code:d.code||String(state.clients.length+1).padStart(6,'0'),document:d.document,phone:d.phone,city:d.city,balance:old?.balance??Number(d.balance||0),notes:d.notes,purchases:old?.purchases||0,total:old?.total||0,photo:photo||old?.photo||'',ledger:old?.ledger||[]};const originalId=x.id,i=state.clients.findIndex(y=>y.id===originalId),saved=await GrassiBackend.saveClient(x);if(saved?.id)x.id=saved.id;i>=0?state.clients[i]=x:state.clients.unshift(x);save();clientAccount(x.id);toast(i>=0?'Cliente actualizado':'Cliente creado')}
  if(f.id==='employeeForm'){if(GrassiBackend.isRemote())throw Error('Para crear accesos en producción use el flujo seguro descrito en docs/PRODUCTION.md');const old=state.employees.find(x=>x.id===f.dataset.id),photo=await fileData(f.elements.photo.files[0]),hasPermissions=Object.keys(d).some(k=>k.startsWith('permission_')),permissions=hasPermissions?{sales:d.permission_sales==='on',clients:d.permission_clients==='on',stock:d.permission_stock==='on',cash:d.permission_cash==='on',reports:d.permission_reports==='on'}:(old?.permissions||{sales:true,clients:true,stock:false,cash:false,reports:false});const x={id:f.dataset.id||`F${Date.now()}`,name:d.name,role:d.role,email:d.email,document:d.document,phone:d.phone,admin:d.admin==='1',supervisor:d.supervisor==='1',permissions,photo:photo||old?.photo||'',sales:old?.sales||0,total:old?.total||0,ticket:old?.ticket||0,goal:old?.goal||0};const i=state.employees.findIndex(y=>y.id===x.id);i>=0?state.employees[i]=x:state.employees.unshift(x);save();closeLayer();render();toast(i>=0?'Funcionario actualizado':'Funcionario creado')}
	  if(f.id==='ledgerForm'){const c=state.clients.find(x=>x.id===f.dataset.id),action=f.dataset.type,requested=Number(d.amount);let type=action==='debit'?'debit':'credit',amount=requested,description=d.description;if(action==='adjust'){const difference=requested-Number(c.balance||0);if(Math.abs(difference)<.005){clientAccount(c.id);return toast('El saldo ya tiene ese valor')}type=difference>0?'credit':'debit';amount=Math.abs(difference);description=`Corrección de saldo — ${description}`}if(action==='payment')description=`Pago ${d.paymentMethod} — ${description}`;if(!Number.isFinite(amount)||amount<=0)throw Error('Ingrese un valor válido');await GrassiBackend.recordLedger(c.id,type,amount,description);c.balance+=type==='credit'?amount:-amount;c.ledger.push({date:new Date(`${d.date}T12:00:00`).toISOString(),type,description,amount});if(action==='payment'){const movement={id:`M${Date.now()}`,date:new Date(`${d.date}T12:00:00`).toISOString(),type:'in',description:`Pago de ${c.name} — ${d.paymentMethod}`,amount,employee:currentEmployee()?.name||authSession.name};const saved=await GrassiBackend.addCashMovement(movement);if(saved?.id)movement.id=saved.id;state.cash.unshift(movement)}save();clientAccount(c.id);toast(action==='payment'?'Pago registrado':'Movimiento registrado')}
	  if(f.id==='paymentForm')await completeSale(ui.payment,d.received);
	  if(f.id==='posNotesForm'){ui.posNotes=d.notes||'';renderPosLayer();toast('Observación guardada')}
	  if(f.id==='posDeliveryForm'){ui.posDelivery=[d.delivery,d.reference].filter(Boolean).join(' · ');renderPosLayer();toast('Entrega guardada')}
	  if(f.id==='cartItemForm'){const item=ui.cart.find(x=>x.id===f.dataset.id),p=item&&state.products.find(x=>x.id===item.id);if(!item)throw Error('Producto no encontrado');const requested=Math.max(1,Number(d.qty)||1),base=Math.max(0,Number(d.price)||0),discount=Math.min(100,Math.max(0,Number(d.discount)||0));item.qty=state.settings.options.blockNoStock?Math.min(Number(p?.stock||requested),requested):requested;item.basePrice=base;item.discount=discount;item.price=base*(1-discount/100);renderPosLayer();toast('Producto actualizado')}
  if(f.id==='cashForm'){ensureCashSession();const movement={id:`M${Date.now()}`,date:new Date().toISOString(),type:d.type,description:d.description,amount:Number(d.amount),employee:currentEmployee()?.name||authSession.name};const saved=await GrassiBackend.addCashMovement(movement);if(saved?.id)movement.id=saved.id;state.cash.unshift(movement);save();closeLayer();render();toast('Transacción registrada')}
  if(f.id==='stockForm'){const p=state.products.find(x=>x.id===f.dataset.id),amount=Number(d.amount);if(!p||!Number.isFinite(amount)||amount<0)throw Error('Ingrese una cantidad válida');const next=d.type==='set'?amount:d.type==='in'?p.stock+amount:p.stock-amount;if(next<0&&state.settings.options.blockNoStock)throw Error('La salida supera el stock disponible');p.stock=Math.max(0,next);await GrassiBackend.saveProduct(p);state.stockMovements=state.stockMovements||[];state.stockMovements.unshift({id:`S${Date.now()}`,date:new Date().toISOString(),productId:p.id,type:d.type,amount,reason:d.reason,employee:currentEmployee()?.name||authSession.name});save();closeLayer();render();toast('Stock actualizado')}
  if(f.id==='brandForm'){state.settings={...state.settings,...d,currency:'Bs'};await GrassiBackend.saveBusiness(state.settings);save();render();toast('Identidad actualizada')}
  if(f.id==='appearanceForm'){const shell=d.shell||'nex',palette=shell==='loop'&&d.palette==='blue'?'purple':d.palette,accent=palettes[palette]||palettes.blue;state.settings.appearance={mode:d.mode,palette,accent,shell};state.settings.accent=accent;await GrassiBackend.saveSettings(state.settings);save();render();toast(shell==='loop'?'Diseño Loop aplicado':'Apariencia aplicada')}
	  if(f.id==='cashClosingForm'){state.settings.cashClosing={automatic:d.automatic==='true',time:d.time||'00:00'};await GrassiBackend.saveSettings(state.settings);save();render();checkAutomaticCashClose();toast('Horario de cierre guardado')}
	  if(f.id==='posLayoutForm'){const items=$$('#posEditorList [data-widget]').map(x=>x.dataset.widget);state.settings.posLayout={dock:d.dock,density:d.density,theme:d.theme||'touch',items};await GrassiBackend.saveSettings(state.settings);save();render();toast('Layout de PDV guardado')}
 }catch(error){toast(error.message||'No fue posible guardar','error')}
});
$('#importInput').addEventListener('change',async e=>{const file=e.target.files[0];if(!file)return;try{const text=await file.text();if(ui.importType==='backup'){const x=JSON.parse(text);if(!x.products||!x.clients)throw Error('Respaldo inválido');state={...structuredClone(seed),...x}}else{const rows=parseCSV(text);if(!rows.length)throw Error('CSV vacío');if(ui.importType==='products')state.products.push(...rows.map((r,i)=>({...r,id:r.id||`P${Date.now()}${i}`,price:Number(r.price),cost:Number(r.cost),wholesale:Number(r.wholesale),stock:Number(r.stock),minStock:Number(r.minStock)})));if(ui.importType==='clients')state.clients.push(...rows.map((r,i)=>{let ledger=[];try{ledger=JSON.parse(r.ledger||'[]')}catch{}return{...r,id:r.id||`C${Date.now()}${i}`,balance:Number(r.balance),purchases:Number(r.purchases),total:Number(r.total),ledger,photo:''}}));if(ui.importType==='employees')state.employees.push(...rows.map((r,i)=>({...r,id:r.id||`F${Date.now()}${i}`,sales:Number(r.sales),total:Number(r.total),ticket:Number(r.ticket),goal:Number(r.goal),admin:r.admin==='true',supervisor:r.supervisor==='true',photo:''}))) }save();render();toast('Datos importados')}catch(err){toast(err.message||'No fue posible importar','error')}e.target.value=''});
$('#menuToggle')?.addEventListener('click',()=>{$('#sidebar').classList.toggle('open');ui.menu=$('#sidebar').classList.contains('open')});
document.addEventListener('dragstart',e=>{const item=e.target.closest('.pos-editor-item');if(!item)return;ui.draggedWidget=item.dataset.widget;item.classList.add('dragging');e.dataTransfer.effectAllowed='move'});
document.addEventListener('dragend',e=>e.target.closest('.pos-editor-item')?.classList.remove('dragging'));
document.addEventListener('dragover',e=>{const item=e.target.closest('.pos-editor-item');if(item)e.preventDefault()});
document.addEventListener('drop',e=>{const target=e.target.closest('.pos-editor-item');if(!target||!ui.draggedWidget)return;e.preventDefault();const source=$(`[data-widget="${ui.draggedWidget}"]`,target.parentElement);if(source&&source!==target)target.parentElement.insertBefore(source,target);ui.draggedWidget=''});
document.addEventListener('keydown',e=>{if(!authSession)return;const inPos=Boolean($('.pdv')),typing=['INPUT','TEXTAREA','SELECT'].includes(e.target.tagName);if(inPos&&e.key==='Enter'&&e.target.id==='posSearch'){e.preventDefault();return runPosCommand('Enter')}if(inPos&&e.ctrlKey&&['ArrowUp','ArrowDown'].includes(e.key)){e.preventDefault();const item=ui.cart.at(-1),product=item&&state.products.find(p=>p.id===item.id);if(!item)return toast('Añada un producto primero','error');const limit=state.settings.options.blockNoStock?Number(product?.stock||1):Infinity;item.qty=Math.max(1,Math.min(limit,item.qty+(e.key==='ArrowUp'?1:-1)));return refreshPosView()}if(inPos&&['F1','F2','F4','F6','F8','F9'].includes(e.key)){e.preventDefault();return runPosCommand(e.key)}if(inPos&&e.key==='Delete'&&!typing){e.preventDefault();return runPosCommand('Supr')}if(e.key==='F3'){e.preventDefault();if(inPos)return $('#posSearch')?.focus();return openPos()}if(e.key==='Escape'&&!$('#layer').hidden){e.preventDefault();if(appMode==='standalone-pos'||ui.cart.length)return renderPosLayer();return closeLayer()}if((e.ctrlKey||e.metaKey)&&e.key.toLowerCase()==='f'){e.preventDefault();(inPos?$('#posSearch'):$('#pageSearch'))?.focus()}});
function refreshNetworkStatus(){const status=queueStatus(),pill=$('#syncPill');if(pill){pill.className=`sync-pill ${status[1]}`;pill.textContent=`● ${status[0]}`}if(navigator.onLine)syncOfflineSales()}
window.addEventListener('online',refreshNetworkStatus);window.addEventListener('offline',refreshNetworkStatus);
async function boot(){applyBrand();injectIcons();if('serviceWorker'in navigator&&location.protocol!=='file:')navigator.serviceWorker.register('./sw.js').catch(()=>{});if(appMode==='standalone-pos'&&sessionStorage.getItem('grassi.pos.unlocked')!=='1')return renderLogin();const restored=await GrassiBackend.restoreSession();if(restored){await startAuthenticated(restored);syncOfflineSales()}else renderLogin()}
setInterval(()=>{if(authSession)checkAutomaticCashClose()},30000);
boot();
